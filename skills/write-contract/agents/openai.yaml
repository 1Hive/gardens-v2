interface:
  display_name: "Gardens Contract Writes"
  short_description: "Prepare Gardens V2 write transactions"
  default_prompt: "Use $write-contract to prepare a Gardens V2 state-changing transaction with the correct target, calldata, value, and chain ID."

policy:
  allow_implicit_invocation: true
