silent: true
version: "3"

dotenv: [".env"]

env:
  RPC_URL_LOCALHOST: http://127.0.0.1:8545

tasks:
  print:
    desc: Print
    cmds:
      - echo ${RPC_URL}
  aggregate-abi:
    desc: Aggregate abi
    cmds:
      - echo "📦 Aggregating diamond ABIs..."
      - node scripts/aggregate-diamond-abi.js
      - echo "✅ ABI aggregation complete"
  aggregate-abi-verbose:
    desc: Aggregate abi verbose
    cmds:
      - echo "📦 Aggregating diamond ABIs (verbose mode)..."
      - node scripts/aggregate-diamond-abi.js --verbose
      - echo "✅ ABI aggregation complete"
  sync-abis:
    desc: Sync ABIs from out
    cmds:
      - ./scripts/sync-abis.sh
  verify-storage:
    desc: Verify storage
    cmds:
      - echo "🔍 Verifying diamond storage layouts..."
      - ./scripts/verify-storage-layout.sh || (echo "❌ Storage verification FAILED! Fix storage alignment before deploying." && exit 1)
      - echo "✅ Storage verification passed"
  verify-storage-quick:
    desc: Verify storage quick
    cmds:
      - echo "🔍 Verifying diamond storage layouts (quick mode)..."
      - ./scripts/verify-storage-layout.sh --skip-build || (echo "❌ Storage verification FAILED! Fix storage alignment before deploying." && exit 1)
      - echo "✅ Storage verification passed"
  verify-storage-verbose:
    desc: Verify storage verbose
    cmds:
      - echo "🔍 Verifying diamond storage layouts (verbose mode)..."
      - ./scripts/verify-storage-layout.sh --verbose || (echo "❌ Storage verification FAILED! Fix storage alignment before deploying." && exit 1)
      - echo "✅ Storage verification passed"
  fork:
    desc: Fork
    cmds:
      - anvil --chain-id 1337
  fork0:
    desc: Fork0
    cmds:
      - anvil --host=0.0.0.0 --chain-id 1337
  fork0b5:
    desc: Fork0b5
    cmds:
      - anvil --host=0.0.0.0 --chain-id 1337 -b 5
  fund_arb:
    desc: Fund arb
    cmds:
      - cmd: |
          cast send \
          --from 0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266 \
          --unlocked 0xefeab1ea32a5d7c6b1de6192ee531a2ef51198d9 \
          --value 100ether \
          --rpc-url=${RPC_URL_LOCALHOST}
  test1:
    desc: Test1
    cmds:
      - cmd: |
          forge test --fork-url ${RPC_URL_LOCALHOST} --mc CVStrategyTest \
          --sender 0xf39fd6e51aad88f6f4ce6ab8827279cfffb92266 -vv
  cast1:
    desc: Cast1
    cmds:
      - cmd: |
          cast send --account pk --rpc-url ${RPC_URL_LOCALHOST} \
          "0xfEcFbe13c84595Ba47464CD4d8d3bb359372Fc6f" "nonce()"
  deploy:
    desc: Deploy
    cmds:
      - cmd: |
          forge script script/DeployCV.s.sol:DeployCV \
          --rpc-url ${RPC_URL_LOCALHOST} \
          --sender 0xf39fd6e51aad88f6f4ce6ab8827279cfffb92266 \
          --unlocked \
          --broadcast -vvvvv
      - task: sync-abis
  add-strategy-local:
    desc: Add strategy local
    cmds:
      - cmd: |
          forge script script/AddStrategy.s.sol:AddStrategy \
          --rpc-url ${RPC_URL_LOCALHOST} \
          --sender 0xf39fd6e51aad88f6f4ce6ab8827279cfffb92266 \
          --unlocked \
          --sig "run(address,address)" ${S} ${C} \
          --broadcast \
          -vvvv
  deploy-testnet-multi-sep:
    desc: Deploy testnet multi sep
    cmds:
      - cmd: |
          forge script script/DeployCVMultiChain.s.sol:DeployCVMultiChain \
          --rpc-url ${RPC_URL_SEP_TESTNET} \
          --account PK_DEPLOYER \
          --sig "run(string)" 'sepolia' \
          --ffi \
          --broadcast \
          --legacy \
          -vvv
      - task: sync-abis
  deploy-core-ethsep:
    desc: Deploy core contracts ethsepolia
    cmds:
      - cmd: |
          forge script script/DeployCoreContracts.s.sol:DeployCoreContracts \
          --rpc-url ${RPC_URL_SEP_TESTNET} \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --sig "run(string)" 'ethsepolia' \
          --ffi \
          --broadcast \
          --legacy \
          -vvv \
          --verify
      - task: sync-abis
  verify-testnet-multi-sep:
    desc: Verify testnet multi sep
    cmds:
      - cmd: |
          forge script script/DeployCVMultiChain.s.sol:DeployCVMultiChain \
          --rpc-url ${RPC_URL_SEP_TESTNET} \
          --account PK_DEPLOYER \
          --sig "run(string)" 'sepolia' \
          --ffi \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --legacy \
          --verify \
          -vvv
        ignore_error: true
      - task: sync-abis
  add-strategy-ethsep:
    desc: Add strategy ethsep
    cmds:
      - cmd: |
          forge script script/AddStrategy.s.sol:AddStrategy \
          --rpc-url ${RPC_URL_SEP_TESTNET} \
          --account PK_DEPLOYER \
          --sig "run(address,address)" ${S} ${C} \
          --broadcast \
          --legacy \
          -vvvv
        ignore_error: true
  add-strategy-ethsep-pool:
    desc: Add strategy ethsep pool
    cmds:
      - cmd: |
          forge script script/AddStrategy.s.sol:AddStrategy \
          --rpc-url ${RPC_URL_SEP_TESTNET} \
          --account PK_DEPLOYER \
          --sig "run(uint256,address)" ${P} ${C} \
          --broadcast \
          --legacy \
          -vvvv
        ignore_error: true
  upgrade-ethsep:
    desc: Upgrade ethsep
    deps:
      - verify-storage
      - task: refresh-facets-ethsep
    cmds:
      - cmd: |
          forge script script/UpgradeCVMultichain.s.sol:UpgradeCVMultichain \
          --rpc-url ${RPC_URL_SEP_TESTNET} \
          --sig "run(string)" 'ethsepolia' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --chain-id 11155111 \
          --ffi \
          --broadcast \
          --verify \
          -vvv
      - task: verify-all-ethsep
  upgrade-ethsep-factory-upgrade-impl:
    desc: Upgrade ethsep factory implementation only
    deps:
      - verify-storage
      - task: refresh-facets-ethsep
    cmds:
      - cmd: |
          forge script script/UpgradeCVMultichain.s.sol:UpgradeCVMultichain \
          --rpc-url ${RPC_URL_SEP_TESTNET} \
          --sig "runFactoryUpgradeImpl(string)" 'ethsepolia' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --chain-id 11155111 \
          --ffi \
          --skip-simulation \
          --broadcast \
          -v
  upgrade-and-verify-ethsep:
    desc: Upgrade and verify ethsep
    deps:
      - verify-storage
      - upgrade-ethsep
      - task: refresh-facets-ethsep
    cmds: []
  refresh-facets-ethsep:
    desc: Redeploy changed facet snapshots and write .FACETS addresses in networks.json (ethsepolia)
    cmds:
      - cmd: |
          forge script script/RefreshFacetSnapshots.s.sol:RefreshFacetSnapshots \
          --rpc-url ${RPC_URL_SEP_TESTNET} \
          --sig "run(string)" 'ethsepolia' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --ffi \
          --broadcast \
          --chain-id 11155111 \
          -vv
  verify-diamond-facets-ethsep:
    desc: Verify live diamond facets on ethsepolia
    cmds:
      - cmd: |
          ./scripts/verify-diamond-facets.sh \
          --network ethsepolia \
          --rpc-url ${RPC_URL_SEP_TESTNET} \
          --chain-id 11155111 \
          --etherscan-key ${ETHERSCAN_API_KEY}
  refresh-facets-and-verify-ethsep:
    desc: Refresh facet snapshots and verify live diamond facets on ethsepolia
    cmds:
      - task: refresh-facets-ethsep
      - task: verify-diamond-facets-ethsep
  refresh-facets-opsep:
    desc: Redeploy changed facet snapshots and write .FACETS addresses in networks.json (opsepolia)
    cmds:
      - cmd: |
          forge script script/RefreshFacetSnapshots.s.sol:RefreshFacetSnapshots \
          --rpc-url ${RPC_URL_OP_TESTNET} \
          --sig "run(string)" 'opsepolia' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --ffi \
          --broadcast \
          --chain-id 11155420 \
          -vv
  verify-diamond-facets-opsep:
    desc: Verify live diamond facets on opsepolia
    cmds:
      - cmd: |
          ./scripts/verify-diamond-facets.sh \
          --network opsepolia \
          --rpc-url ${RPC_URL_OP_TESTNET} \
          --chain-id 11155420 \
          --etherscan-key ${ETHERSCAN_API_KEY}
  refresh-facets-and-verify-opsep:
    desc: Refresh facet snapshots and verify live diamond facets on opsepolia
    cmds:
      - task: refresh-facets-opsep
      - task: verify-diamond-facets-opsep
  refresh-facets-arbsep:
    desc: Redeploy changed facet snapshots and write .FACETS addresses in networks.json (arbsepolia)
    cmds:
      - cmd: |
          forge script script/RefreshFacetSnapshots.s.sol:RefreshFacetSnapshots \
          --rpc-url ${RPC_URL_ARB_TESTNET} \
          --sig "run(string)" 'arbsepolia' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --ffi \
          --broadcast \
          --chain-id 421614 \
          -vv
  verify-diamond-facets-arbsep:
    desc: Verify live diamond facets on arbsepolia
    cmds:
      - cmd: |
          ./scripts/verify-diamond-facets.sh \
          --network arbsepolia \
          --rpc-url ${RPC_URL_ARB_TESTNET} \
          --chain-id 421614 \
          --etherscan-key ${ETHERSCAN_API_KEY}
  refresh-facets-and-verify-arbsep:
    desc: Refresh facet snapshots and verify live diamond facets on arbsepolia
    cmds:
      - task: refresh-facets-arbsep
      - task: verify-diamond-facets-arbsep
  refresh-facets-arbitrum:
    desc: Redeploy changed facet snapshots and write .FACETS addresses in networks.json (arbitrum)
    cmds:
      - cmd: |
          forge script script/RefreshFacetSnapshots.s.sol:RefreshFacetSnapshots \
          --rpc-url ${RPC_URL_ARB} \
          --sig "run(string)" 'arbitrum' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --ffi \
          --broadcast \
          --chain-id 42161 \
          -vv
  verify-diamond-facets-arbitrum:
    desc: Verify live diamond facets on arbitrum
    cmds:
      - cmd: |
          ./scripts/verify-diamond-facets.sh \
          --network arbitrum \
          --rpc-url ${RPC_URL_ARB} \
          --chain-id 42161 \
          --etherscan-key ${ETHERSCAN_API_KEY}
  refresh-facets-and-verify-arbitrum:
    desc: Refresh facet snapshots and verify live diamond facets on arbitrum
    cmds:
      - task: refresh-facets-arbitrum
      - task: verify-diamond-facets-arbitrum
  refresh-facets-optimism:
    desc: Redeploy changed facet snapshots and write .FACETS addresses in networks.json (optimism)
    cmds:
      - cmd: |
          forge script script/RefreshFacetSnapshots.s.sol:RefreshFacetSnapshots \
          --rpc-url ${RPC_URL_OPT} \
          --sig "run(string)" 'optimism' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --ffi \
          --broadcast \
          --chain-id 10 \
          -vv
  verify-diamond-facets-optimism:
    desc: Verify live diamond facets on optimism
    cmds:
      - cmd: |
          ./scripts/verify-diamond-facets.sh \
          --network optimism \
          --rpc-url ${RPC_URL_OPT} \
          --chain-id 10 \
          --etherscan-key ${ETHERSCAN_API_KEY}
  refresh-facets-and-verify-optimism:
    desc: Refresh facet snapshots and verify live diamond facets on optimism
    cmds:
      - task: refresh-facets-optimism
      - task: verify-diamond-facets-optimism
  refresh-facets-polygon:
    desc: Redeploy changed facet snapshots and write .FACETS addresses in networks.json (polygon)
    cmds:
      - cmd: |
          forge script script/RefreshFacetSnapshots.s.sol:RefreshFacetSnapshots \
          --rpc-url ${RPC_URL_POLYGON} \
          --sig "run(string)" 'polygon' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --ffi \
          --broadcast \
          --chain-id 137 \
          -vv
  verify-diamond-facets-polygon:
    desc: Verify live diamond facets on polygon
    cmds:
      - cmd: |
          ./scripts/verify-diamond-facets.sh \
          --network polygon \
          --rpc-url ${RPC_URL_POLYGON} \
          --chain-id 137 \
          --etherscan-key ${ETHERSCAN_API_KEY}
  refresh-facets-and-verify-polygon:
    desc: Refresh facet snapshots and verify live diamond facets on polygon
    cmds:
      - task: refresh-facets-polygon
      - task: verify-diamond-facets-polygon
  refresh-facets-gnosis:
    desc: Redeploy changed facet snapshots and write .FACETS addresses in networks.json (gnosis)
    cmds:
      - cmd: |
          forge script script/RefreshFacetSnapshots.s.sol:RefreshFacetSnapshots \
          --rpc-url ${RPC_URL_GNOSIS} \
          --sig "run(string)" 'gnosis' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --ffi \
          --broadcast \
          --chain-id 100 \
          -vv
  verify-diamond-facets-gnosis:
    desc: Verify live diamond facets on gnosis
    cmds:
      - cmd: |
          ./scripts/verify-diamond-facets.sh \
          --network gnosis \
          --rpc-url ${RPC_URL_GNOSIS} \
          --chain-id 100 \
          --etherscan-key ${ETHERSCAN_API_KEY}
  refresh-facets-and-verify-gnosis:
    desc: Refresh facet snapshots and verify live diamond facets on gnosis
    cmds:
      - task: refresh-facets-gnosis
      - task: verify-diamond-facets-gnosis
  refresh-facets-base:
    desc: Redeploy changed facet snapshots and write .FACETS addresses in networks.json (base)
    cmds:
      - cmd: |
          forge script script/RefreshFacetSnapshots.s.sol:RefreshFacetSnapshots \
          --rpc-url ${RPC_URL_BASE} \
          --sig "run(string)" 'base' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --ffi \
          --broadcast \
          --chain-id 8453 \
          -vv
  verify-diamond-facets-base:
    desc: Verify live diamond facets on base
    cmds:
      - cmd: |
          ./scripts/verify-diamond-facets.sh \
          --network base \
          --rpc-url ${RPC_URL_BASE} \
          --chain-id 8453 \
          --etherscan-key ${ETHERSCAN_API_KEY}
  refresh-facets-and-verify-base:
    desc: Refresh facet snapshots and verify live diamond facets on base
    cmds:
      - task: refresh-facets-base
      - task: verify-diamond-facets-base
  refresh-facets-celo:
    desc: Redeploy changed facet snapshots and write .FACETS addresses in networks.json (celo)
    cmds:
      - cmd: |
          forge script script/RefreshFacetSnapshots.s.sol:RefreshFacetSnapshots \
          --rpc-url ${RPC_URL_CELO} \
          --sig "run(string)" 'celo' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --ffi \
          --broadcast \
          --chain-id 42220 \
          -vv
  verify-diamond-facets-celo:
    desc: Verify live diamond facets on celo
    cmds:
      - cmd: |
          ./scripts/verify-diamond-facets.sh \
          --network celo \
          --rpc-url ${RPC_URL_CELO} \
          --chain-id 42220 \
          --etherscan-key ${ETHERSCAN_API_KEY}
  refresh-facets-and-verify-celo:
    desc: Refresh facet snapshots and verify live diamond facets on celo
    cmds:
      - task: refresh-facets-celo
      - task: verify-diamond-facets-celo
  deploy-global-pause-controller-ethsep:
    desc: Deploy global pause controller ethsep
    cmds:
      - cmd: |
          forge script script/DeployGlobalPauseController.s.sol:DeployGlobalPauseController \
          --rpc-url ${RPC_URL_SEP_TESTNET} \
          --sig "run(string)" 'ethsepolia' \
          --chain-id 11155111 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --verify \
          --broadcast \
          -vv
        ignore_error: true

  refresh-facets-mainnet:
    desc: Redeploy changed facet snapshots and write .FACETS addresses in networks.json (mainnet)
    cmds:
      - cmd: |
          forge script script/RefreshFacets.s.sol:RefreshFacets \
          --rpc-url ${RPC_URL_MAINNET} \
          --sig "run(string)" 'mainnet' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --ffi \
          --chain-id 1 \
          --broadcast \
          -vvv
        ignore_error: true
      - task: sync-abis
  verify-diamond-facets-mainnet:
    desc: Verify live diamond facets on mainnet
    cmds:
      - cmd: |
          ./scripts/verify-diamond-facets.sh \
          --network mainnet \
          --rpc-url ${RPC_URL_MAINNET} \
          --chain-id 1 \
          --etherscan-key ${ETHERSCAN_API_KEY}
  refresh-facets-and-verify-mainnet:
    desc: Refresh facet snapshots and verify live diamond facets on mainnet
    cmds:
      - task: refresh-facets-mainnet
      - task: verify-diamond-facets-mainnet
  verify-all-ethsep:
    desc: Run full deployment verification on ethsepolia
    cmds:
      - cmd: |
          bash ./scripts/verify-all-deployments.sh --scope all --network ethsepolia
  verify-all-opsep:
    desc: Run full deployment verification on opsepolia
    cmds:
      - cmd: |
          bash ./scripts/verify-all-deployments.sh --scope all --network opsepolia
  verify-all-arbsep:
    desc: Run full deployment verification on arbsepolia
    cmds:
      - cmd: |
          bash ./scripts/verify-all-deployments.sh --scope all --network arbsepolia
  verify-all-arbitrum:
    desc: Run full deployment verification on arbitrum
    cmds:
      - cmd: |
          bash ./scripts/verify-all-deployments.sh --scope all --network arbitrum
  verify-all-optimism:
    desc: Run full deployment verification on optimism
    cmds:
      - cmd: |
          bash ./scripts/verify-all-deployments.sh --scope all --network optimism
  verify-all-polygon:
    desc: Run full deployment verification on polygon
    cmds:
      - cmd: |
          bash ./scripts/verify-all-deployments.sh --scope all --network polygon
  verify-all-gnosis:
    desc: Run full deployment verification on gnosis
    cmds:
      - cmd: |
          bash ./scripts/verify-all-deployments.sh --scope all --network gnosis
  verify-all-base:
    desc: Run full deployment verification on base
    cmds:
      - cmd: |
          bash ./scripts/verify-all-deployments.sh --scope all --network base
  verify-all-celo:
    desc: Run full deployment verification on celo
    cmds:
      - cmd: |
          bash ./scripts/verify-all-deployments.sh --scope all --network celo
  verify-all-mainnet:
    desc: Run full deployment verification on mainnet
    cmds:
      - cmd: |
          bash ./scripts/verify-all-deployments.sh --scope all --network mainnet
  deploy-streaming-escrow-factory-ethsep:
    desc: Deploy streaming escrow factory ethsep (set on registry factory)
    cmds:
      - cmd: |
          RESUME_FLAG=""
          if [ "${RESUME:-false}" = "true" ]; then
            RESUME_FLAG="--resume"
          fi
          forge script script/DeployOrUpgradeStreamingEscrows.s.sol:DeployOrUpgradeStreamingEscrows \
          --rpc-url ${RPC_URL_SEP_TESTNET} \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --sig "run(string,address,bool)" 'ethsepolia' ${I:-0x0000000000000000000000000000000000000000} {{default "false" .DEPLOY_IMPLEMENTATION_ONLY}} \
          --ffi \
          --broadcast \
          ${RESUME_FLAG} \
          --verify \
          --chain-id 11155111 \
          -vvv
  upgrade-streaming-escrows-ethsep:
    desc: Deploy or upgrade streaming escrows on ethsep (networks.json managed in script, optional I implementation)
    cmds:
      - cmd: |
          forge script script/DeployOrUpgradeStreamingEscrows.s.sol:DeployOrUpgradeStreamingEscrows \
          --rpc-url ${RPC_URL_SEP_TESTNET} \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --sig "run(string,address,bool)" 'ethsepolia' ${I:-0x0000000000000000000000000000000000000000} {{default "false" .DEPLOY_IMPLEMENTATION_ONLY}} \
          --ffi \
          --broadcast \
          --verify \
          --chain-id 11155111 \
          -vvv
  deploy-streaming-escrow-factory-arbsep:
    desc: Deploy streaming escrow factory arbsep (set on registry factory)
    cmds:
      - cmd: |
          RESUME_FLAG=""
          if [ "${RESUME:-false}" = "true" ]; then
            RESUME_FLAG="--resume"
          fi
          forge script script/DeployOrUpgradeStreamingEscrows.s.sol:DeployOrUpgradeStreamingEscrows \
          --rpc-url ${RPC_URL_ARB_TESTNET} \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --sig "run(string,address,bool)" 'arbsepolia' ${I:-0x0000000000000000000000000000000000000000} {{default "false" .DEPLOY_IMPLEMENTATION_ONLY}} \
          --ffi \
          --broadcast \
          ${RESUME_FLAG} \
          --verify \
          --chain-id 421614 \
          -vvv
  deploy-token-opsep:
    desc: Deploy token opsep
    cmds:
      - cmd: |
          forge script script/DeployERC20.s.sol:DeployERC20 \
          --rpc-url ${RPC_URL_OP_TESTNET} \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --sig "run(string,string)" 'Honey' 'HNY' \
          --ffi \
          --broadcast \
          --chain-id 11155420 \
          --verify
        ignore_error: true
      - task: sync-abis
  fork-testnet-opsep:
    desc: Fork testnet opsep
    cmds:
      - anvil --host=0.0.0.0 -f ${RPC_URL_OP_TESTNET}
  deploy-verify-opsep:
    desc: Deploy verify opsep
    cmds:
      - cmd: |
          forge verify-contract \
          --etherscan-api-key ${ETHERSCAN_API_KEY}
        ignore_error: true
  deploy-opsep:
    desc: Deploy opsep
    cmds:
      - cmd: |
          forge script script/DeployCVMultiChain.s.sol:DeployCVMultiChain \
          --rpc-url ${RPC_URL_OP_TESTNET} \
          --sig "run(string)" 'opsepolia' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --chain-id 11155420 \
          --ffi \
          --broadcast \
          --verify \
          -vv
        ignore_error: true
      - task: sync-abis
  upgrade-opsep:
    desc: Upgrade opsep
    deps:
      - verify-storage
      - task: refresh-facets-opsep
    cmds:
      - cmd: |
          forge script script/UpgradeCVMultichain.s.sol:UpgradeCVMultichain \
          --rpc-url ${RPC_URL_OP_TESTNET} \
          --sig "run(string)" 'opsepolia' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --chain-id 11155420 \
          --ffi \
          --broadcast \
          --verify \
          -vvv
      - task: verify-all-opsep
  upgrade-opsep-factory:
    desc: Upgrade opsep factory
    deps:
      - verify-storage
      - task: refresh-facets-opsep
    cmds:
      - cmd: |
          forge script script/UpgradeCVMultichain.s.sol:UpgradeCVMultichain \
          --rpc-url ${RPC_URL_OP_TESTNET} \
          --sig "runFactory(string)" 'opsepolia' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --chain-id 11155420 \
          --ffi \
          --skip-simulation \
          --broadcast \
          -v
  upgrade-opsep-factory-upgrade-impl:
    desc: Upgrade opsep factory implementation only
    deps:
      - verify-storage
      - task: refresh-facets-opsep
    cmds:
      - cmd: |
          forge script script/UpgradeCVMultichain.s.sol:UpgradeCVMultichain \
          --rpc-url ${RPC_URL_OP_TESTNET} \
          --sig "runFactoryUpgradeImpl(string)" 'opsepolia' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --chain-id 11155420 \
          --ffi \
          --skip-simulation \
          --broadcast \
          -v
  upgrade-opsep-factory-set-community-facets:
    desc: Set opsep factory community facets only
    deps:
      - verify-storage
      - task: refresh-facets-opsep
    cmds:
      - cmd: |
          SPLIT_FACTORY_FACETS=true forge script script/UpgradeCVMultichain.s.sol:UpgradeCVMultichain \
          --rpc-url ${RPC_URL_OP_TESTNET} \
          --sig "runFactorySetCommunityFacets(string)" 'opsepolia' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --chain-id 11155420 \
          --ffi \
          --skip-simulation \
          --broadcast \
          -v
  upgrade-opsep-factory-set-strategy-facets:
    desc: Set opsep factory strategy facets only
    deps:
      - verify-storage
      - task: refresh-facets-opsep
    cmds:
      - cmd: |
          SPLIT_FACTORY_FACETS=true forge script script/UpgradeCVMultichain.s.sol:UpgradeCVMultichain \
          --rpc-url ${RPC_URL_OP_TESTNET} \
          --sig "runFactorySetStrategyFacets(string)" 'opsepolia' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --chain-id 11155420 \
          --ffi \
          --skip-simulation \
          --broadcast \
          -v
  upgrade-opsep-factory-set-pause-controller:
    desc: Set opsep factory pause controller only
    deps:
      - verify-storage
      - task: refresh-facets-opsep
    cmds:
      - cmd: |
          forge script script/UpgradeCVMultichain.s.sol:UpgradeCVMultichain \
          --rpc-url ${RPC_URL_OP_TESTNET} \
          --sig "runFactorySetPauseController(string)" 'opsepolia' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --chain-id 11155420 \
          --ffi \
          --skip-simulation \
          --broadcast \
          -v
  upgrade-opsep-factory-set-registry-template:
    desc: Set opsep factory registry template only
    deps:
      - verify-storage
      - task: refresh-facets-opsep
    cmds:
      - cmd: |
          forge script script/UpgradeCVMultichain.s.sol:UpgradeCVMultichain \
          --rpc-url ${RPC_URL_OP_TESTNET} \
          --sig "runFactorySetRegistryTemplate(string)" 'opsepolia' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --chain-id 11155420 \
          --ffi \
          --skip-simulation \
          --broadcast \
          -v
  upgrade-opsep-factory-set-strategy-template:
    desc: Set opsep factory strategy template only
    deps:
      - verify-storage
      - task: refresh-facets-opsep
    cmds:
      - cmd: |
          forge script script/UpgradeCVMultichain.s.sol:UpgradeCVMultichain \
          --rpc-url ${RPC_URL_OP_TESTNET} \
          --sig "runFactorySetStrategyTemplate(string)" 'opsepolia' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --chain-id 11155420 \
          --ffi \
          --skip-simulation \
          --broadcast \
          -v
  upgrade-opsep-communities:
    desc: Upgrade opsep communities
    deps:
      - verify-storage
      - task: refresh-facets-opsep
    cmds:
      - cmd: |
          forge script script/UpgradeCVMultichain.s.sol:UpgradeCVMultichain \
          --rpc-url ${RPC_URL_OP_TESTNET} \
          --sig "run(string,string)" 'opsepolia' 'communities' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --chain-id 11155420 \
          --ffi \
          --broadcast \
          -vvv
  upgrade-opsep-strategies:
    desc: Upgrade opsep strategies
    deps:
      - verify-storage
      - task: refresh-facets-opsep
    cmds:
      - cmd: |
          forge script script/UpgradeCVMultichain.s.sol:UpgradeCVMultichain \
          --rpc-url ${RPC_URL_OP_TESTNET} \
          --sig "run(string,string)" 'opsepolia' 'strategies' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --chain-id 11155420 \
          --ffi \
          --broadcast \
          -vvv
  upgrade-diamond-opsep:
    desc: Upgrade diamond opsep
    deps:
      - verify-storage
      - task: refresh-facets-opsep
    cmds:
      - cmd: |
          forge script script/UpgradeCVDiamond.s.sol:UpgradeCVDiamond \
          --rpc-url ${RPC_URL_OP_TESTNET} \
          --sig "run(string)" 'opsepolia' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --chain-id 11155420 \
          --broadcast \
          --ffi \
          --verify \
          --slow \
          -vvv
  upgrade-community-diamond-opsep:
    desc: Upgrade community diamond opsep
    deps:
      - verify-storage
      - task: refresh-facets-opsep
    cmds:
      - cmd: |
          forge script script/UpgradeRegistryCommunityDiamond.s.sol:UpgradeRegistryCommunityDiamond \
          --rpc-url ${RPC_URL_OP_TESTNET} \
          --sig "run(string)" 'opsepolia' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --chain-id 11155420 \
          --ffi \
          --broadcast \
          --verify \
          -vvv
  verify-diamond-blockscout-opsep:
    desc: Verify diamond blockscout opsep
    cmds:
      - cmd: |
          forge script script/UpgradeCVDiamond.s.sol:UpgradeCVDiamondTest \
          --rpc-url ${RPC_URL_OP_TESTNET} \
          --sig "run(string)" 'opsepolia' \
          --account PK_DEPLOYER \
          --verifier blockscout \
          --verifier-url https://optimism.blockscout.com/api/ \
          --chain-id 11155420 \
          --ffi \
          --verify \
          --slow
        ignore_error: true
  verify-community-diamond-blockscout-opsep:
    desc: Verify community diamond blockscout opsep
    cmds:
      - cmd: |
          forge script script/UpgradeRegistryCommunityDiamond.s.sol:UpgradeRegistryCommunityDiamond \
          --rpc-url ${RPC_URL_OP_TESTNET} \
          --sig "run(string)" 'opsepolia' \
          --account PK_DEPLOYER \
          --verifier blockscout \
          --verifier-url https://optimism.blockscout.com/api/ \
          --chain-id 11155420 \
          --ffi \
          --verify \
          --slow
        ignore_error: true
  upgrade-all-diamond-opsep:
    desc: Upgrade all diamond opsep
    deps:
      - verify-storage
      - upgrade-diamond-opsep
      - upgrade-community-diamond-opsep
      - task: refresh-facets-opsep
    cmds: []
  upgrade-atomic-diamond-opsep:
    desc: Upgrade atomic diamond opsep
    deps:
      - verify-storage
      - task: refresh-facets-opsep
    cmds:
      - cmd: |
          forge script script/UpgradeAllDiamonds.s.sol:UpgradeAllDiamonds \
          --rpc-url ${RPC_URL_OP_TESTNET} \
          --sig "run(string)" 'opsepolia' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --chain-id 11155420 \
          --broadcast \
          --ffi \
          --verify \
          --slow \
          --via-ir \
          -vvv
  upgrade-and-verify-diamond-opsep:
    desc: Upgrade and verify diamond opsep
    deps:
      - verify-storage
      - upgrade-diamond-opsep
      - verify-diamond-blockscout-opsep
      - task: refresh-facets-opsep
    cmds: []
  upgrade-and-verify-community-diamond-opsep:
    desc: Upgrade and verify community diamond opsep
    deps:
      - verify-storage
      - upgrade-community-diamond-opsep
      - verify-community-diamond-blockscout-opsep
      - task: refresh-facets-opsep
    cmds: []
  verify-blockscout-opsep:
    desc: Verify blockscout opsep
    cmds:
      - cmd: |
          forge script script/UpgradeCVMultichain.s.sol:UpgradeCVMultichain \
          --rpc-url ${RPC_URL_OP_TESTNET} \
          --sig "run(string)" 'opsepolia' \
          --account PK_DEPLOYER \
          --verifier blockscout \
          --verifier-url https://optimism.blockscout.com/api/ \
          --chain-id 11155420 \
          --ffi \
          --verify \
          --slow
        ignore_error: true
  verify-etherscan-opsep:
    desc: Verify etherscan opsep
    cmds:
      - cmd: |
          forge script script/UpgradeCVMultichain.s.sol:UpgradeCVMultichain \
          --rpc-url ${RPC_URL_OP_TESTNET} \
          --sig "run(string)" 'opsepolia' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --chain-id 11155420 \
          --ffi \
          --verify \
          --slow
        ignore_error: true
  deploy-new-collateral-vault-template-opsep:
    desc: Deploy new collateral vault template opsep
    cmds:
      - cmd: |
          forge script script/DeployCollateralVaultTemplate.s.sol:DeployCollateralVaultTemplate \
          --rpc-url ${RPC_URL_OP_TESTNET} \
          --sig "run(string)" 'opsepolia' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --ffi \
          --chain-id 11155420 \
          --legacy \
          --broadcast \
          --with-gas-price 1301660000 \
          --verify \
          -vvv
        ignore_error: true
      - task: sync-abis
  upgrade-and-verify-opsep:
    desc: Upgrade and verify opsep
    deps:
      - verify-storage
      - upgrade-opsep
      - verify-etherscan-opsep
      - task: refresh-facets-opsep
    cmds: []
  transfer-ownership-opsep:
    desc: Transfer ownership opsep
    cmds:
      - cmd: |
          forge script script/TransferOwnershipAllContracts.s.sol:TransferOwnershipAllContracts \
          --rpc-url ${RPC_URL_OP_TESTNET} \
          --account PK_DEPLOYER \
          --sig "run(string)" 'opsepolia' \
          --chain-id 11155420 \
          --broadcast \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --verify \
          -vvvv
        ignore_error: true
  deploy-passport-scorer-opsep:
    desc: Deploy passport scorer opsep
    cmds:
      - cmd: |
          forge script script/DeployPassportScorer.s.sol:DeployPassportScorer \
          --rpc-url ${RPC_URL_OP_TESTNET} \
          --sig "run(string)" 'opsepolia' \
          --chain-id 11155420 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --verify \
          --broadcast \
          -vv
        ignore_error: true
      - task: sync-abis
  deploy-or-upgrade-global-pause-controller-opsep:
    desc: Deploy or upgrade global pause controller opsep
    cmds:
      - cmd: |
          forge script script/DeployGlobalPauseController.s.sol:DeployGlobalPauseController \
          --rpc-url ${RPC_URL_OP_TESTNET} \
          --sig "run(string)" 'opsepolia' \
          --chain-id 11155420 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --broadcast \
          -vv \
          --verify
        ignore_error: true
      - task: sync-abis
  deploy-streaming-escrow-factory-opsep:
    desc: Deploy streaming escrow factory opsep (set on registry factory)
    cmds:
      - cmd: |
          RESUME_FLAG=""
          if [ "${RESUME:-false}" = "true" ]; then
            RESUME_FLAG="--resume"
          fi
          forge script script/DeployOrUpgradeStreamingEscrows.s.sol:DeployOrUpgradeStreamingEscrows \
          --rpc-url ${RPC_URL_OP_TESTNET} \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --sig "run(string,address,bool)" 'opsepolia' ${I:-0x0000000000000000000000000000000000000000} {{default "false" .DEPLOY_IMPLEMENTATION_ONLY}} \
          --ffi \
          --broadcast \
          ${RESUME_FLAG} \
          --verify \
          --chain-id 11155420 \
          -vvv
  upgrade-streaming-escrows-opsep:
    desc: Deploy or upgrade streaming escrows on opsep (networks.json managed in script, optional I implementation)
    cmds:
      - cmd: |
          forge script script/DeployOrUpgradeStreamingEscrows.s.sol:DeployOrUpgradeStreamingEscrows \
          --rpc-url ${RPC_URL_OP_TESTNET} \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --sig "run(string,address)" 'opsepolia' ${I:-0x0000000000000000000000000000000000000000} \
          --ffi \
          --broadcast \
          --verify \
          --chain-id 11155420 \
          -vvv
  upgrade-streaming-escrows-arbsep:
    desc: Deploy or upgrade streaming escrows on arbsep (networks.json managed in script, optional I implementation)
    cmds:
      - cmd: |
          forge script script/DeployOrUpgradeStreamingEscrows.s.sol:DeployOrUpgradeStreamingEscrows \
          --rpc-url ${RPC_URL_ARB_TESTNET} \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --sig "run(string,address,bool)" 'arbsepolia' ${I:-0x0000000000000000000000000000000000000000} {{default "false" .DEPLOY_IMPLEMENTATION_ONLY}} \
          --ffi \
          --broadcast \
          --verify \
          --chain-id 421614 \
          -vvv
  deploy-snapshot-strategy-facets-opsep:
    desc: Deploy and snapshot strategy facets opsep
    cmds:
      - cmd: |
          forge script script/DeploySnapshotFacets.s.sol:DeploySnapshotFacets \
          --rpc-url ${RPC_URL_OP_TESTNET} \
          --sig "run(string,string)" 'opsepolia' 'strategy' \
          --chain-id 11155420 \
          --account PK_DEPLOYER \
          --ffi \
          --broadcast \
          -vvv
  deploy-facets-opsep:
    desc: Deploy and snapshot all facets opsep
    cmds:
      - task: deploy-snapshot-strategy-facets-opsep
      - task: deploy-snapshot-community-facets-opsep
  deploy-snapshot-community-facets-opsep:
    desc: Deploy and snapshot community facets opsep
    cmds:
      - cmd: |
          forge script script/DeploySnapshotFacets.s.sol:DeploySnapshotFacets \
          --rpc-url ${RPC_URL_OP_TESTNET} \
          --sig "run(string,string)" 'opsepolia' 'community' \
          --chain-id 11155420 \
          --account PK_DEPLOYER \
          --ffi \
          --broadcast \
          -vvv
  deploy-safe-arbitrator-opsep:
    desc: Deploy safe arbitrator opsep
    cmds:
      - cmd: |
          forge script script/DeploySafeArbitrator.s.sol:DeploySafeArbitrator \
          --rpc-url ${RPC_URL_OP_TESTNET} \
          --sig "run(string)" 'opsepolia' \
          --chain-id 11155420 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --verify \
          --broadcast \
          -vv
        ignore_error: true
      - task: sync-abis
  deploy-or-upgrade-good-dollar-sybil-opsep:
    desc: Deploy or upgrade good dollar sybil opsep
    cmds:
      - cmd: |
          forge script script/DeployGoodDollarSybil.s.sol:DeployGoodDollarSybil \
          --rpc-url ${RPC_URL_OP_TESTNET} \
          --sig "run(string)" 'opsepolia' \
          --chain-id 11155420 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --verify \
          --broadcast \
          -vv
        ignore_error: true
      - task: sync-abis
  fork-testnet-arbsep:
    desc: Fork testnet arbsep
    cmds:
      - anvil --host=0.0.0.0 -f ${RPC_URL_ARB_TESTNET}
  deploy-verify-arbsep:
    desc: Deploy verify arbsep
    cmds:
      - cmd: |
          forge verify-contract \
          --etherscan-api-key ${ETHERSCAN_API_KEY}
        ignore_error: true
  deploy-arbsep:
    desc: Deploy arbsep
    cmds:
      - cmd: |
          forge script script/DeployCVMultiChain.s.sol:DeployCVMultiChain \
          --rpc-url ${RPC_URL_ARB_TESTNET} \
          --sig "run(string)" 'arbsepolia' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --chain-id 421614 \
          --ffi \
          --broadcast \
          --verify \
          -vv
        ignore_error: true
      - task: sync-abis
  upgrade-arbsep:
    desc: Upgrade arbsep
    deps:
      - verify-storage
      - task: refresh-facets-arbsep
    cmds:
      - task: deploy-facets-arbsep
      - task: upgrade-arbsep-factory
      - task: upgrade-arbsep-communities
      - task: upgrade-arbsep-strategies
      - task: verify-all-arbsep
  upgrade-arbsep-factory:
    desc: Upgrade arbsep factory
    deps:
      - verify-storage
      - task: refresh-facets-arbsep
    cmds:
      - cmd: |
          forge script script/UpgradeCVMultichain.s.sol:UpgradeCVMultichain \
          --rpc-url ${RPC_URL_ARB_TESTNET} \
          --sig "runFactory(string)" 'arbsepolia' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --chain-id 421614 \
          --ffi \
          --skip-simulation \
          --broadcast \
          -v
  upgrade-arbsep-factory-upgrade-impl:
    desc: Upgrade arbsep factory implementation only
    deps:
      - verify-storage
      - task: refresh-facets-arbsep
    cmds:
      - cmd: |
          forge script script/UpgradeCVMultichain.s.sol:UpgradeCVMultichain \
          --rpc-url ${RPC_URL_ARB_TESTNET} \
          --sig "runFactoryUpgradeImpl(string)" 'arbsepolia' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --chain-id 421614 \
          --ffi \
          --skip-simulation \
          --broadcast \
          -v
  upgrade-arbsep-factory-set-community-facets:
    desc: Set arbsep factory community facets only
    deps:
      - verify-storage
      - task: refresh-facets-arbsep
    cmds:
      - cmd: |
          SPLIT_FACTORY_FACETS=true forge script script/UpgradeCVMultichain.s.sol:UpgradeCVMultichain \
          --rpc-url ${RPC_URL_ARB_TESTNET} \
          --sig "runFactorySetCommunityFacets(string)" 'arbsepolia' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --chain-id 421614 \
          --ffi \
          --skip-simulation \
          --broadcast \
          -v
  upgrade-arbsep-factory-set-strategy-facets:
    desc: Set arbsep factory strategy facets only
    deps:
      - verify-storage
      - task: refresh-facets-arbsep
    cmds:
      - cmd: |
          SPLIT_FACTORY_FACETS=true forge script script/UpgradeCVMultichain.s.sol:UpgradeCVMultichain \
          --rpc-url ${RPC_URL_ARB_TESTNET} \
          --sig "runFactorySetStrategyFacets(string)" 'arbsepolia' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --chain-id 421614 \
          --ffi \
          --skip-simulation \
          --broadcast \
          -v
  upgrade-arbsep-factory-set-pause-controller:
    desc: Set arbsep factory pause controller only
    deps:
      - verify-storage
      - task: refresh-facets-arbsep
    cmds:
      - cmd: |
          forge script script/UpgradeCVMultichain.s.sol:UpgradeCVMultichain \
          --rpc-url ${RPC_URL_ARB_TESTNET} \
          --sig "runFactorySetPauseController(string)" 'arbsepolia' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --chain-id 421614 \
          --ffi \
          --skip-simulation \
          --broadcast \
          -v
  upgrade-arbsep-factory-set-registry-template:
    desc: Set arbsep factory registry template only
    deps:
      - verify-storage
      - task: refresh-facets-arbsep
    cmds:
      - cmd: |
          forge script script/UpgradeCVMultichain.s.sol:UpgradeCVMultichain \
          --rpc-url ${RPC_URL_ARB_TESTNET} \
          --sig "runFactorySetRegistryTemplate(string)" 'arbsepolia' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --chain-id 421614 \
          --ffi \
          --skip-simulation \
          --broadcast \
          -v
  upgrade-arbsep-factory-set-strategy-template:
    desc: Set arbsep factory strategy template only
    deps:
      - verify-storage
      - task: refresh-facets-arbsep
    cmds:
      - cmd: |
          forge script script/UpgradeCVMultichain.s.sol:UpgradeCVMultichain \
          --rpc-url ${RPC_URL_ARB_TESTNET} \
          --sig "runFactorySetStrategyTemplate(string)" 'arbsepolia' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --chain-id 421614 \
          --ffi \
          --skip-simulation \
          --broadcast \
          -v
  upgrade-arbsep-communities:
    desc: Upgrade arbsep communities
    deps:
      - verify-storage
      - task: refresh-facets-arbsep
    cmds:
      - cmd: |
          forge script script/UpgradeCVMultichain.s.sol:UpgradeCVMultichain \
          --rpc-url ${RPC_URL_ARB_TESTNET} \
          --sig "run(string,string)" 'arbsepolia' 'communities' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --chain-id 421614 \
          --ffi \
          --broadcast \
          -vvv
  upgrade-arbsep-strategies:
    desc: Upgrade arbsep strategies
    deps:
      - verify-storage
      - task: refresh-facets-arbsep
    cmds:
      - cmd: |
          forge script script/UpgradeCVMultichain.s.sol:UpgradeCVMultichain \
          --rpc-url ${RPC_URL_ARB_TESTNET} \
          --sig "run(string,string)" 'arbsepolia' 'strategies' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --chain-id 421614 \
          --ffi \
          --broadcast \
          -vvv
  upgrade-atomic-diamond-arbsep:
    desc: Upgrade atomic diamond arbsep
    deps:
      - verify-storage
      - task: refresh-facets-arbsep
    cmds:
      - cmd: |
          forge script script/UpgradeAllDiamonds.s.sol:UpgradeAllDiamonds \
          --rpc-url ${RPC_URL_ARB_TESTNET} \
          --sig "run(string)" 'arbsepolia' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --chain-id 421614 \
          --broadcast \
          --ffi \
          --verify \
          --slow \
          --via-ir
  verify-blockscout-arbsep:
    desc: Verify blockscout arbsep
    cmds:
      - cmd: |
          forge script script/UpgradeCVMultichain.s.sol:UpgradeCVMultichain \
          --rpc-url ${RPC_URL_ARB_TESTNET} \
          --account PK_DEPLOYER \
          --verifier blockscout \
          --verifier-url https://arbitrum.blockscout.com/api/ \
          --verify \
          --slow
        ignore_error: true
  deploy-verify:
    desc: Deploy verify
    cmds:
      - cmd: |
          forge script script/DeployCVArbSepolia.s.sol:DeployCVArbSepolia \
          --rpc-url ${RPC_URL_ARB_TESTNET} \
          --account PK_DEPLOYER \
          --verify \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          -vv
        ignore_error: true
      - task: sync-abis
  deploy-pool-testnet:
    desc: Deploy pool testnet
    cmds:
      - cmd: |
          forge script script/DeployCVArbSepoliaPool.s.sol:DeployCVArbSepoliaPool \
          --rpc-url ${RPC_URL_ARB_TESTNET} \
          --account PK_DEPLOYER \
          --verify \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --broadcast -vv
        ignore_error: true
      - task: sync-abis
  deploy-commfee-testnet:
    desc: Deploy commfee testnet
    cmds:
      - cmd: |
          forge script script/DeployCVArbSepoliaCommFee.s.sol:DeployCVArbSepoliaCommFee \
          --rpc-url ${RPC_URL_ARB_TESTNET} \
          --account PK_DEPLOYER \
          --verify \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --broadcast -vv
        ignore_error: true
      - task: sync-abis
  deploy-testnet:
    desc: Deploy testnet
    cmds:
      - cmd: |
          forge script script/DeployCVArbSepolia.s.sol:DeployCVArbSepolia \
          --rpc-url ${RPC_URL_ARB_TESTNET} \
          --account PK_DEPLOYER \
          --verify \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --broadcast \
          -vv
        ignore_error: true
      - task: sync-abis
  create_safe_arbsep:
    desc: Create safe arbsep
    cmds:
      - cmd: |
          forge script script/CreateSafe.s.sol \
          --rpc-url ${RPC_URL_ARB_TESTNET} \
          --account PK_DEPLOYER \
          --sig "run()" \
          --broadcast \
          --verify \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --chain-id 421614 \
          -vvvv
        ignore_error: true
  deploy-testnet-dry:
    desc: Deploy testnet dry
    cmds:
      - cmd: |
          forge script script/DeployCVArbSepolia.s.sol:DeployCVArbSepolia \
          --rpc-url ${RPC_URL_ARB_TESTNET} \
          --account PK_DEPLOYER \
          -vvvv
        ignore_error: true
      - task: sync-abis
  add-owner:
    desc: Add owner
    cmds:
      - cmd: |
          forge script script/AddOwner.s.sol:AddOwner \
          --rpc-url ${RPC_URL_ARB_TESTNET} \
          --account PK_DEPLOYER \
          --sig "run(address,address)" ${owner} ${safe} \
          --broadcast \
          -vvvv
        ignore_error: true
  add-strategy:
    desc: Add strategy
    cmds:
      - cmd: |
          forge script script/AddStrategy.s.sol:AddStrategy \
          --rpc-url ${RPC_URL_ARB_TESTNET} \
          --account PK_DEPLOYER \
          --sig "run(address,address)" ${S} ${C} \
          --broadcast \
          -vvvv
        ignore_error: true
  deploy-test-arb:
    desc: Deploy test arb
    cmds:
      - cmd: |
          forge script script/VerifyTest.s.sol:VerifyTest \
          --rpc-url ${RPC_URL_ARB_TESTNET} \
          --sig "run(string)" 'arbsepolia' \
          --account PK_DEPLOYER \
          --ffi \
          --chain-id 421614 \
          --broadcast \
          --legacy \
          --force \
          --verify \
          -vvv
        ignore_error: true
  deploy-new-collateral-vault-template-arbitrum-sepolia:
    desc: Deploy new collateral vault template arbitrum sepolia
    cmds:
      - cmd: |
          forge script script/DeployCollateralVaultTemplate.s.sol:DeployCollateralVaultTemplate \
          --rpc-url ${RPC_URL_ARB_TESTNET} \
          --sig "run(string)" 'arbsepolia' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --ffi \
          --chain-id 421614 \
          --legacy \
          --broadcast \
          --with-gas-price 1301660000 \
          --verify \
          -vvv
        ignore_error: true
      - task: sync-abis
  upgrade-and-verify-arbsep:
    desc: Upgrade and verify arbsep
    deps:
      - verify-storage
      - upgrade-arbsep
      - verify-blockscout-arbsep
      - task: refresh-facets-arbsep
    cmds: []
  transfer-ownership-arbsep:
    desc: Transfer ownership arbsep
    cmds:
      - cmd: |
          forge script script/TransferOwnershipAllContracts.s.sol:TransferOwnershipAllContracts \
          --rpc-url ${RPC_URL_ARB_TESTNET} \
          --account PK_DEPLOYER \
          --sig "run(string)" 'arbsepolia' \
          --chain-id 421614 \
          --broadcast \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --verify \
          -vvvv
        ignore_error: true
  deploy-passport-scorer-arbsep:
    desc: Deploy passport scorer arbsep
    cmds:
      - cmd: |
          forge script script/DeployPassportScorer.s.sol:DeployPassportScorer \
          --rpc-url ${RPC_URL_ARB_TESTNET} \
          --sig "run(string)" 'arbsepolia' \
          --chain-id 421614 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --verify \
          --broadcast \
          -vv
        ignore_error: true
      - task: sync-abis
  deploy-global-pause-controller-arbsep:
    desc: Deploy global pause controller arbsep
    cmds:
      - cmd: |
          forge script script/DeployGlobalPauseController.s.sol:DeployGlobalPauseController \
          --rpc-url ${RPC_URL_ARB_TESTNET} \
          --sig "run(string)" 'arbsepolia' \
          --chain-id 421614 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --verify \
          --broadcast \
          -vv
        ignore_error: true
      - task: sync-abis
  deploy-or-upgrade-if-exists-safe-arbitrator-arbsep:
    desc: Deploy or upgrade if exists safe arbitrator arbsep
    cmds:
      - cmd: |
          forge script script/DeploySafeArbitrator.s.sol:DeploySafeArbitrator \
          --rpc-url ${RPC_URL_ARB_TESTNET} \
          --sig "run(string)" 'arbsepolia' \
          --chain-id 421614 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --verify \
          --broadcast \
          -vv
        ignore_error: true
      - task: sync-abis
  deploy-or-upgrade-good-dollar-sybil-arbsep:
    desc: Deploy or upgrade good dollar sybil arbsep
    cmds:
      - cmd: |
          forge script script/DeployGoodDollarSybil.s.sol:DeployGoodDollarSybil \
          --rpc-url ${RPC_URL_ARB_TESTNET} \
          --sig "run(string)" 'arbsepolia' \
          --chain-id 421614 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --verify \
          --broadcast \
          -vv
        ignore_error: true
      - task: sync-abis
  deploy-snapshot-strategy-facets-arbsep:
    desc: Deploy and snapshot strategy facets arbsep
    cmds:
      - cmd: |
          forge script script/DeploySnapshotFacets.s.sol:DeploySnapshotFacets \
          --rpc-url ${RPC_URL_ARB_TESTNET} \
          --sig "run(string,string)" 'arbsepolia' 'strategy' \
          --chain-id 421614 \
          --account PK_DEPLOYER \
          --ffi \
          --broadcast \
          -vvv
        ignore_error: true
  deploy-facets-arbsep:
    desc: Deploy and snapshot all facets arbsep
    cmds:
      - task: deploy-snapshot-strategy-facets-arbsep
      - task: deploy-snapshot-community-facets-arbsep
  deploy-snapshot-community-facets-arbsep:
    desc: Deploy and snapshot community facets arbsep
    cmds:
      - cmd: |
          forge script script/DeploySnapshotFacets.s.sol:DeploySnapshotFacets \
          --rpc-url ${RPC_URL_ARB_TESTNET} \
          --sig "run(string,string)" 'arbsepolia' 'community' \
          --chain-id 421614 \
          --account PK_DEPLOYER \
          --ffi \
          --broadcast \
          -vvv
        ignore_error: true
  transfer-ownership-arbitrum:
    desc: Transfer ownership arbitrum
    cmds:
      - cmd: |
          forge script script/TransferOwnershipAllContracts.s.sol:TransferOwnershipAllContracts \
          --rpc-url ${RPC_URL_ARB} \
          --account PK_DEPLOYER \
          --sig "run(string)" 'arbitrum' \
          --chain-id 42161 \
          --broadcast \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --verify \
          -vvv
        ignore_error: true
  deploy-new-collateral-vault-template-arbitrum:
    desc: Deploy new collateral vault template arbitrum
    cmds:
      - cmd: |
          forge script script/DeployCollateralVaultTemplate.s.sol:DeployCollateralVaultTemplate \
          --rpc-url ${RPC_URL_ARB} \
          --sig "run(string)" 'arbitrum' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --ffi \
          --chain-id 42161 \
          --legacy \
          --broadcast \
          --verify \
          -vvv
        ignore_error: true
      - task: sync-abis
  deploy-arbitrum:
    desc: Deploy arbitrum
    cmds:
      - cmd: |
          forge script script/DeployCVMultiChain.s.sol:DeployCVMultiChain \
          --rpc-url ${RPC_URL_ARB} \
          --sig "run(string)" 'arbitrum' \
          --account PK_DEPLOYER \
          --ffi \
          --chain-id 42161 \
          --broadcast \
          --legacy \
          -vvv
        ignore_error: true
      - task: sync-abis
  upgrade-arbitrum:
    desc: Upgrade arbitrum
    deps:
      - verify-storage
      - task: refresh-facets-arbitrum
    cmds:
      - cmd: |
          forge script script/UpgradeCVMultichain.s.sol:UpgradeCVMultichain \
          --rpc-url ${RPC_URL_ARB} \
          --sig "run(string)" 'arbitrum' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --ffi \
          --chain-id 42161 \
          --broadcast \
          --slow \
          -vvv
      - task: verify-all-arbitrum
  upgrade-atomic-diamond-arbitrum:
    desc: Upgrade atomic diamond arbitrum
    deps:
      - verify-storage
      - task: refresh-facets-arbitrum
    cmds:
      - cmd: |
          forge script script/UpgradeAllDiamonds.s.sol:UpgradeAllDiamonds \
          --rpc-url ${RPC_URL_ARB} \
          --sig "run(string)" 'arbitrum' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --chain-id 42161 \
          --broadcast \
          --ffi \
          --slow \
          --via-ir
  verify-blockscout-arbitrum:
    desc: Verify blockscout arbitrum
    cmds:
      - cmd: |
          forge script script/UpgradeCVMultichain.s.sol:UpgradeCVMultichain \
          --rpc-url ${RPC_URL_ARB} \
          --sig "run(string)" 'arbitrum' \
          --account PK_DEPLOYER \
          --verifier blockscout \
          --verifier-url https://arbitrum.blockscout.com/api/ \
          --ffi \
          --chain-id 42161 \
          --legacy \
          --verify \
          -vvv \
          --slow
        ignore_error: true
  verify-deploy-blockscout-arbitrum:
    desc: Verify deploy blockscout arbitrum
    cmds:
      - cmd: |
          forge script script/DeployCVMultiChain.s.sol:DeployCVMultiChain \
          --rpc-url ${RPC_URL_ARB} \
          --sig "run(string)" 'arbitrum' \
          --account PK_DEPLOYER \
          --verifier blockscout \
          --verifier-url https://arbitrum.blockscout.com/api/ \
          --ffi \
          --chain-id 42161 \
          --legacy \
          --verify \
          -vvv \
          --slow
        ignore_error: true
      - task: sync-abis
  deploy-and-verify-arbitrum:
    desc: Deploy and verify arbitrum
    deps:
      - deploy-arbitrum
      - verify-deploy-blockscout-arbitrum
    cmds: []
  upgrade-and-verify-arbitrum:
    desc: Upgrade and verify arbitrum
    deps:
      - verify-storage
      - task: refresh-facets-arbitrum
    cmds:
      - task: upgrade-arbitrum
      - task: verify-blockscout-arbitrum
  change-passport-manager-arbitrum:
    desc: Change passport manager arbitrum
    cmds:
      - cmd: |
          forge script script/PassportScorerWriter.s.sol:PassportScorerWriter \
          --rpc-url ${RPC_URL_ARB} \
          --sig "run(string)" 'arbitrum' \
          --chain-id 42161 \
          --account PK_DEPLOYER \
          --ffi \
          --broadcast \
          --legacy \
          -vvv
        ignore_error: true
  deploy-passport-scorer-arbitrum:
    desc: Deploy passport scorer arbitrum
    cmds:
      - cmd: |
          forge script script/DeployPassportScorer.s.sol:DeployPassportScorer \
          --rpc-url ${RPC_URL_ARB} \
          --sig "run(string)" 'arbitrum' \
          --chain-id 42161 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --verify \
          --broadcast \
          -vv
        ignore_error: true
      - task: sync-abis
  deploy-global-pause-controller-arbitrum:
    desc: Deploy global pause controller arbitrum
    cmds:
      - cmd: |
          forge script script/DeployGlobalPauseController.s.sol:DeployGlobalPauseController \
          --rpc-url ${RPC_URL_ARB} \
          --sig "run(string)" 'arbitrum' \
          --chain-id 42161 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --verify \
          --broadcast \
          -vv
        ignore_error: true
      - task: sync-abis
  deploy-or-upgrade-global-pause-controller-arbitrum:
    desc: Deploy or upgrade global pause controller arbitrum
    cmds:
      - cmd: |
          forge script script/DeployGlobalPauseController.s.sol:DeployGlobalPauseController \
          --rpc-url ${RPC_URL_ARB} \
          --sig "run(string)" 'arbitrum' \
          --chain-id 42161 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --broadcast \
          -vv \
          --verify
        ignore_error: true
      - task: sync-abis
  verify-global-pause-controller-arbitrum:
    desc: Verify global pause controller arbitrum
    cmds:
      - cmd: |
          bash ./scripts/verify-global-pause-controller.sh \
          --network arbitrum \
          --rpc-url ${RPC_URL_ARB} \
          --chain-id 42161 \
          --etherscan-key ${ETHERSCAN_API_KEY} \
          --verifier-url https://arbitrum.blockscout.com/api/
  deploy-or-upgrade-if-exists-safe-arbitrator-arbitrum:
    desc: Deploy or upgrade if exists safe arbitrator arbitrum
    cmds:
      - cmd: |
          forge script script/DeploySafeArbitrator.s.sol:DeploySafeArbitrator \
          --rpc-url ${RPC_URL_ARB} \
          --sig "run(string)" 'arbitrum' \
          --chain-id 42161 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --verify \
          --broadcast \
          -vv
        ignore_error: true
      - task: sync-abis
  deploy-streaming-escrow-factory-arbitrum:
    desc: Deploy streaming escrow factory arbitrum (set on registry factory)
    cmds:
      - cmd: |
          RESUME_FLAG=""
          if [ "${RESUME:-false}" = "true" ]; then
            RESUME_FLAG="--resume"
          fi
          forge script script/DeployOrUpgradeStreamingEscrows.s.sol:DeployOrUpgradeStreamingEscrows \
          --rpc-url ${RPC_URL_ARB} \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --sig "run(string,address,bool)" 'arbitrum' ${I:-0x0000000000000000000000000000000000000000} {{default "false" .DEPLOY_IMPLEMENTATION_ONLY}} \
          --ffi \
          --broadcast \
          ${RESUME_FLAG} \
          --verify \
          --legacy \
          --chain-id 42161 \
          -vvv
  verify-streaming-escrow-factory-arbitrum:
    desc: Verify streaming escrow factory arbitrum
    cmds:
      - cmd: |
          bash ./scripts/verify-streaming-escrow-factory.sh \
          --network arbitrum \
          --rpc-url ${RPC_URL_ARB} \
          --chain-id 42161 \
          --etherscan-key ${ETHERSCAN_API_KEY} \
          --verifier-url https://arbitrum.blockscout.com/api/
  upgrade-streaming-escrows-arbitrum:
    desc: Deploy or upgrade streaming escrows on arbitrum (networks.json managed in script, optional I implementation)
    cmds:
      - cmd: |
          forge script script/DeployOrUpgradeStreamingEscrows.s.sol:DeployOrUpgradeStreamingEscrows \
          --rpc-url ${RPC_URL_ARB} \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --sig "run(string,address)" 'arbitrum' ${I:-0x0000000000000000000000000000000000000000} \
          --ffi \
          --broadcast \
          --chain-id 42161 \
          -vvv
  deploy-or-upgrade-good-dollar-sybil-arbitrum:
    desc: Deploy or upgrade good dollar sybil arbitrum
    cmds:
      - cmd: |
          forge script script/DeployGoodDollarSybil.s.sol:DeployGoodDollarSybil \
          --rpc-url ${RPC_URL_ARB} \
          --sig "run(string)" 'arbitrum' \
          --chain-id 42161 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --verify \
          --broadcast \
          -vv
        ignore_error: true
      - task: sync-abis
  transfer-ownership-optimism:
    desc: Transfer ownership optimism
    cmds:
      - cmd: |
          forge script script/TransferOwnershipAllContracts.s.sol:TransferOwnershipAllContracts \
          --rpc-url ${RPC_URL_OPT} \
          --sig "run(string)" 'optimism' \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --chain-id 10 \
          --account PK_DEPLOYER \
          --broadcast \
          --verify \
          -vvv
        ignore_error: true
  deploy-new-collateral-vault-template-optimism:
    desc: Deploy new collateral vault template optimism
    cmds:
      - cmd: |
          forge script script/DeployCollateralVaultTemplate.s.sol:DeployCollateralVaultTemplate \
          --rpc-url ${RPC_URL_OPT} \
          --sig "run(string)" 'optimism' \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --chain-id 10 \
          --account PK_DEPLOYER \
          --ffi \
          --legacy \
          --broadcast \
          --verify \
          -vvv
        ignore_error: true
      - task: sync-abis
  deploy-optimism:
    desc: Deploy optimism
    cmds:
      - cmd: |
          forge script script/DeployCVMultiChain.s.sol:DeployCVMultiChain \
          --rpc-url ${RPC_URL_OPT} \
          --sig "run(string)" 'optimism' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --ffi \
          --chain-id 10 \
          --broadcast \
          --legacy \
          --slow \
          --verify \
          -vvv
        ignore_error: true
      - task: sync-abis
  upgrade-optimism:
    desc: Upgrade optimism
    deps:
      - verify-storage
      - task: refresh-facets-optimism
    cmds:
      - cmd: |
          forge script script/UpgradeCVMultichain.s.sol:UpgradeCVMultichain \
          --rpc-url ${RPC_URL_OPT} \
          --sig "run(string)" 'optimism' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --ffi \
          --chain-id 10 \
          --broadcast \
          --legacy \
          -vvv
      - task: verify-all-optimism
  upgrade-atomic-diamond-optimism:
    desc: Upgrade atomic diamond optimism
    deps:
      - verify-storage
      - task: refresh-facets-optimism
    cmds:
      - cmd: |
          forge script script/UpgradeAllDiamonds.s.sol:UpgradeAllDiamonds \
          --rpc-url ${RPC_URL_OPT} \
          --sig "run(string)" 'optimism' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --chain-id 10 \
          --broadcast \
          --ffi \
          --slow \
          --via-ir
  verify-blockscout-optimism:
    desc: Verify blockscout optimism
    cmds:
      - cmd: |
          forge script script/UpgradeCVMultichain.s.sol:UpgradeCVMultichain \
          --rpc-url ${RPC_URL_OPT} \
          --sig "run(string)" 'optimism' \
          --account PK_DEPLOYER \
          --verifier blockscout \
          --verifier-url https://optimism.blockscout.com/api/ \
          --chain-id 10 \
          --legacy \
          --verify \
          -vvv \
          --slow
        ignore_error: true
  verify-deploy-blockscout-optimism:
    desc: Verify deploy blockscout optimism
    cmds:
      - cmd: |
          forge script script/DeployCVMultiChain.s.sol:DeployCVMultiChain \
          --rpc-url ${RPC_URL_OPT} \
          --sig "run(string)" 'optimism' \
          --account PK_DEPLOYER \
          --verifier blockscout \
          --verifier-url https://optimism.blockscout.com/api/ \
          --ffi \
          --chain-id 10 \
          --legacy \
          --verify \
          -vvv \
          --slow
        ignore_error: true
      - task: sync-abis
  deploy-and-verify-optimism:
    desc: Deploy and verify optimism
    deps:
      - deploy-optimism
      - verify-deploy-blockscout-optimism
    cmds: []
  upgrade-and-verify-optimism:
    desc: Upgrade and verify optimism
    deps:
      - verify-storage
      - task: refresh-facets-optimism
    cmds:
      - task: upgrade-optimism
      - task: verify-blockscout-optimism
  change-passport-manager-optimism:
    desc: Change passport manager optimism
    cmds:
      - cmd: |
          forge script script/PassportScorerWriter.s.sol:PassportScorerWriter \
          --rpc-url ${RPC_URL_OPT} \
          --sig "run(string)" 'optimism' \
          --chain-id 10 \
          --account PK_DEPLOYER \
          --ffi \
          --broadcast \
          --legacy \
          -vvv
        ignore_error: true
  deploy-passport-scorer-optimism:
    desc: Deploy passport scorer optimism
    cmds:
      - cmd: |
          forge script script/DeployPassportScorer.s.sol:DeployPassportScorer \
          --rpc-url ${RPC_URL_OPT} \
          --sig "run(string)" 'optimism' \
          --chain-id 10 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --verify \
          --broadcast \
          -vv
        ignore_error: true
      - task: sync-abis
  deploy-global-pause-controller-optimism:
    desc: Deploy global pause controller optimism
    cmds:
      - cmd: |
          forge script script/DeployGlobalPauseController.s.sol:DeployGlobalPauseController \
          --rpc-url ${RPC_URL_OPT} \
          --sig "run(string)" 'optimism' \
          --chain-id 10 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --verify \
          --broadcast \
          -vv
        ignore_error: true
      - task: sync-abis
  deploy-or-upgrade-global-pause-controller-optimism:
    desc: Deploy or upgrade global pause controller optimism
    cmds:
      - cmd: |
          forge script script/DeployGlobalPauseController.s.sol:DeployGlobalPauseController \
          --rpc-url ${RPC_URL_OPT} \
          --sig "run(string)" 'optimism' \
          --chain-id 10 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --broadcast \
          -vv \
          --verify
        ignore_error: true
      - task: sync-abis
  verify-global-pause-controller-optimism:
    desc: Verify global pause controller optimism
    cmds:
      - cmd: |
          bash ./scripts/verify-global-pause-controller.sh \
          --network optimism \
          --rpc-url ${RPC_URL_OPT} \
          --chain-id 10 \
          --etherscan-key ${ETHERSCAN_API_KEY} \
          --verifier-url https://optimism.blockscout.com/api/
  deploy-or-upgrade-if-exists-safe-arbitrator-optimism:
    desc: Deploy or upgrade if exists safe arbitrator optimism
    cmds:
      - cmd: |
          forge script script/DeploySafeArbitrator.s.sol:DeploySafeArbitrator \
          --rpc-url ${RPC_URL_OPT} \
          --sig "run(string)" 'optimism' \
          --chain-id 10 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --verify \
          --broadcast \
          -vv
        ignore_error: true
      - task: sync-abis
  deploy-streaming-escrow-factory-optimism:
    desc: Deploy streaming escrow factory optimism (set on registry factory)
    cmds:
      - cmd: |
          RESUME_FLAG=""
          if [ "${RESUME:-false}" = "true" ]; then
            RESUME_FLAG="--resume"
          fi
          forge script script/DeployOrUpgradeStreamingEscrows.s.sol:DeployOrUpgradeStreamingEscrows \
          --rpc-url ${RPC_URL_OPT} \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --sig "run(string,address,bool)" 'optimism' ${I:-0x0000000000000000000000000000000000000000} {{default "false" .DEPLOY_IMPLEMENTATION_ONLY}} \
          --ffi \
          --broadcast \
          ${RESUME_FLAG} \
          --verify \
          --legacy \
          --chain-id 10 \
          -vvv
  verify-streaming-escrow-factory-optimism:
    desc: Verify streaming escrow factory optimism
    cmds:
      - cmd: |
          bash ./scripts/verify-streaming-escrow-factory.sh \
          --network optimism \
          --rpc-url ${RPC_URL_OPT} \
          --chain-id 10 \
          --etherscan-key ${ETHERSCAN_API_KEY} \
          --verifier-url https://optimism.blockscout.com/api/
  upgrade-streaming-escrows-optimism:
    desc: Deploy or upgrade streaming escrows on optimism (networks.json managed in script, optional I implementation)
    cmds:
      - cmd: |
          forge script script/DeployOrUpgradeStreamingEscrows.s.sol:DeployOrUpgradeStreamingEscrows \
          --rpc-url ${RPC_URL_OPT} \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --sig "run(string,address)" 'optimism' ${I:-0x0000000000000000000000000000000000000000} \
          --ffi \
          --broadcast \
          --chain-id 10 \
          -vvv
  deploy-or-upgrade-good-dollar-sybil-optimism:
    desc: Deploy or upgrade good dollar sybil optimism
    cmds:
      - cmd: |
          forge script script/DeployGoodDollarSybil.s.sol:DeployGoodDollarSybil \
          --rpc-url ${RPC_URL_OPT} \
          --sig "run(string)" 'optimism' \
          --chain-id 10 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --verify \
          --broadcast \
          -vv
        ignore_error: true
      - task: sync-abis
  upgrade-diamond-polygon:
    desc: Upgrade diamond polygon
    deps:
      - verify-storage
      - task: refresh-facets-polygon
    cmds:
      - cmd: |
          forge script script/UpgradeCVDiamond.s.sol:UpgradeCVDiamond \
          --rpc-url ${RPC_URL_POLYGON} \
          --sig "run(string)" 'polygon' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --chain-id 137 \
          --broadcast \
          --ffi \
          --slow \
          -vvv
  upgrade-community-diamond-polygon:
    desc: Upgrade community diamond polygon
    deps:
      - verify-storage
      - task: refresh-facets-polygon
    cmds:
      - cmd: |
          forge script script/UpgradeRegistryCommunityDiamond.s.sol:UpgradeRegistryCommunityDiamond \
          --rpc-url ${RPC_URL_POLYGON} \
          --sig "run(string)" 'polygon' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --chain-id 137 \
          --broadcast \
          --legacy \
          --ffi \
          --slow \
          -vvv
  upgrade-atomic-diamond-polygon:
    desc: Upgrade atomic diamond polygon
    deps:
      - verify-storage
      - task: refresh-facets-polygon
    cmds:
      - cmd: |
          forge script script/UpgradeAllDiamonds.s.sol:UpgradeAllDiamonds \
          --rpc-url ${RPC_URL_POLYGON} \
          --sig "run(string)" 'polygon' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --chain-id 137 \
          --broadcast \
          --legacy \
          --ffi \
          --slow \
          --via-ir \
          -vvv
  transfer-ownership-polygon:
    desc: Transfer ownership polygon
    cmds:
      - cmd: |
          forge script script/TransferOwnershipAllContracts.s.sol:TransferOwnershipAllContracts \
          --rpc-url ${RPC_URL_POLYGON} \
          --sig "run(string)" 'polygon' \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --chain-id 137 \
          --account PK_DEPLOYER \
          --broadcast \
          --verify \
          -vvv
        ignore_error: true
  deploy-new-collateral-vault-template-polygon:
    desc: Deploy new collateral vault template polygon
    cmds:
      - cmd: |
          forge script script/DeployCollateralVaultTemplate.s.sol:DeployCollateralVaultTemplate \
          --rpc-url ${RPC_URL_POLYGON} \
          --sig "run(string)" 'polygon' \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --chain-id 137 \
          --account PK_DEPLOYER \
          --ffi \
          --legacy \
          --broadcast \
          --verify \
          -vvv
        ignore_error: true
      - task: sync-abis
  deploy-polygon:
    desc: Deploy polygon
    cmds:
      - cmd: |
          forge script script/DeployCVMultiChain.s.sol:DeployCVMultiChain \
          --rpc-url ${RPC_URL_POLYGON} \
          --sig "run(string)" 'polygon' \
          --account PK_DEPLOYER \
          --ffi \
          --chain-id 137 \
          --broadcast \
          --legacy \
          -vvv
        ignore_error: true
      - task: sync-abis
  upgrade-polygon:
    desc: Upgrade polygon
    deps:
      - verify-storage
      - task: refresh-facets-polygon
    cmds:
      - cmd: |
          forge script script/UpgradeCVMultichain.s.sol:UpgradeCVMultichain \
          --rpc-url ${RPC_URL_POLYGON} \
          --sig "run(string)" 'polygon' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --ffi \
          --chain-id 137 \
          --broadcast \
          --legacy \
          --slow \
          -vvv
      - task: verify-all-polygon
  verify-blockscout-polygon:
    desc: Verify blockscout polygon
    cmds:
      - cmd: |
          forge script script/UpgradeCVMultichain.s.sol:UpgradeCVMultichain \
          --rpc-url ${RPC_URL_POLYGON} \
          --sig "run(string)" 'polygon' \
          --account PK_DEPLOYER \
          --verifier blockscout \
          --verifier-url https://polygon.blockscout.com/api/ \
          --ffi \
          --chain-id 137 \
          --legacy \
          --verify \
          -vvv \
          --slow
        ignore_error: true
  verify-deploy-blockscout-polygon:
    desc: Verify deploy blockscout polygon
    cmds:
      - cmd: |
          forge script script/DeployCVMultiChain.s.sol:DeployCVMultiChain \
          --rpc-url ${RPC_URL_POLYGON} \
          --sig "run(string)" 'polygon' \
          --account PK_DEPLOYER \
          --verifier blockscout \
          --verifier-url https://polygon.blockscout.com/api/ \
          --ffi \
          --chain-id 137 \
          --legacy \
          --verify \
          -vvv \
          --slow
        ignore_error: true
      - task: sync-abis
  deploy-and-verify-polygon:
    desc: Deploy and verify polygon
    deps:
      - deploy-polygon
      - verify-deploy-blockscout-polygon
    cmds: []
  upgrade-and-verify-polygon:
    desc: Upgrade and verify polygon
    deps:
      - verify-storage
      - task: refresh-facets-polygon
    cmds:
      - task: upgrade-polygon
      - task: verify-blockscout-polygon
  change-passport-manager-polygon:
    desc: Change passport manager polygon
    cmds:
      - cmd: |
          forge script script/PassportScorerWriter.s.sol:PassportScorerWriter \
          --rpc-url ${RPC_URL_POLYGON} \
          --sig "run(string)" 'polygon' \
          --chain-id 137 \
          --account PK_DEPLOYER \
          --ffi \
          --broadcast \
          --legacy \
          -vvv
        ignore_error: true
  deploy-passport-scorer-polygon:
    desc: Deploy passport scorer polygon
    cmds:
      - cmd: |
          forge script script/DeployPassportScorer.s.sol:DeployPassportScorer \
          --rpc-url ${RPC_URL_POLYGON} \
          --sig "run(string)" 'polygon' \
          --chain-id 137 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --verify \
          --broadcast \
          -vv
        ignore_error: true
      - task: sync-abis
  deploy-global-pause-controller-polygon:
    desc: Deploy global pause controller polygon
    cmds:
      - cmd: |
          forge script script/DeployGlobalPauseController.s.sol:DeployGlobalPauseController \
          --rpc-url ${RPC_URL_POLYGON} \
          --sig "run(string)" 'polygon' \
          --chain-id 137 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --verify \
          --broadcast \
          -vv
        ignore_error: true
      - task: sync-abis
  deploy-or-upgrade-global-pause-controller-polygon:
    desc: Deploy or upgrade global pause controller polygon
    cmds:
      - cmd: |
          forge script script/DeployGlobalPauseController.s.sol:DeployGlobalPauseController \
          --rpc-url ${RPC_URL_POLYGON} \
          --sig "run(string)" 'polygon' \
          --chain-id 137 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --broadcast \
          -vv \
          --verify
        ignore_error: true
      - task: sync-abis
  verify-global-pause-controller-polygon:
    desc: Verify global pause controller polygon
    cmds:
      - cmd: |
          bash ./scripts/verify-global-pause-controller.sh \
          --network polygon \
          --rpc-url ${RPC_URL_POLYGON} \
          --chain-id 137 \
          --etherscan-key ${ETHERSCAN_API_KEY} \
          --verifier-url https://polygon.blockscout.com/api/
  deploy-or-upgrade-if-exists-safe-arbitrator-polygon:
    desc: Deploy or upgrade if exists safe arbitrator polygon
    cmds:
      - cmd: |
          forge script script/DeploySafeArbitrator.s.sol:DeploySafeArbitrator \
          --rpc-url ${RPC_URL_POLYGON} \
          --sig "run(string)" 'polygon' \
          --chain-id 137 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --verify \
          --broadcast \
          -vv
        ignore_error: true
      - task: sync-abis
  deploy-streaming-escrow-factory-polygon:
    desc: Deploy streaming escrow factory polygon (set on registry factory)
    cmds:
      - cmd: |
          RESUME_FLAG=""
          if [ "${RESUME:-false}" = "true" ]; then
            RESUME_FLAG="--resume"
          fi
          forge script script/DeployOrUpgradeStreamingEscrows.s.sol:DeployOrUpgradeStreamingEscrows \
          --rpc-url ${RPC_URL_POLYGON} \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --sig "run(string,address,bool)" 'polygon' ${I:-0x0000000000000000000000000000000000000000} {{default "false" .DEPLOY_IMPLEMENTATION_ONLY}} \
          --ffi \
          --broadcast \
          ${RESUME_FLAG} \
          --verify \
          --legacy \
          --chain-id 137 \
          -vvv
  verify-streaming-escrow-factory-polygon:
    desc: Verify streaming escrow factory polygon
    cmds:
      - cmd: |
          bash ./scripts/verify-streaming-escrow-factory.sh \
          --network polygon \
          --rpc-url ${RPC_URL_POLYGON} \
          --chain-id 137 \
          --etherscan-key ${ETHERSCAN_API_KEY} \
          --verifier-url https://polygon.blockscout.com/api/
  upgrade-streaming-escrows-polygon:
    desc: Deploy or upgrade streaming escrows on polygon (networks.json managed in script, optional I implementation)
    cmds:
      - cmd: |
          forge script script/DeployOrUpgradeStreamingEscrows.s.sol:DeployOrUpgradeStreamingEscrows \
          --rpc-url ${RPC_URL_POLYGON} \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --sig "run(string,address)" 'polygon' ${I:-0x0000000000000000000000000000000000000000} \
          --ffi \
          --broadcast \
          --chain-id 137 \
          -vvv
  deploy-or-upgrade-good-dollar-sybil-polygon:
    desc: Deploy or upgrade good dollar sybil polygon
    cmds:
      - cmd: |
          forge script script/DeployGoodDollarSybil.s.sol:DeployGoodDollarSybil \
          --rpc-url ${RPC_URL_POLYGON} \
          --sig "run(string)" 'polygon' \
          --chain-id 137 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --verify \
          --broadcast \
          -vv
        ignore_error: true
      - task: sync-abis
  transfer-ownership-gnosis:
    desc: Transfer ownership gnosis
    cmds:
      - cmd: |
          forge script script/TransferOwnershipAllContracts.s.sol:TransferOwnershipAllContracts \
          --rpc-url ${RPC_URL_GNOSIS} \
          --sig "run(string)" 'gnosis' \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --chain-id 100 \
          --account PK_DEPLOYER \
          --broadcast \
          --verify \
          -vvv
        ignore_error: true
  deploy-new-collateral-vault-template-gnosis:
    desc: Deploy new collateral vault template gnosis
    cmds:
      - cmd: |
          forge script script/DeployCollateralVaultTemplate.s.sol:DeployCollateralVaultTemplate \
          --rpc-url ${RPC_URL_GNOSIS} \
          --sig "run(string)" 'gnosis' \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --chain-id 100 \
          --account PK_DEPLOYER \
          --ffi \
          --legacy \
          --broadcast \
          --verify \
          --slow \
          -vvv
        ignore_error: true
      - task: sync-abis
  deploy-gnosis:
    desc: Deploy gnosis
    cmds:
      - cmd: |
          forge script script/DeployCVMultiChain.s.sol:DeployCVMultiChain \
          --rpc-url ${RPC_URL_GNOSIS} \
          --sig "run(string)" 'gnosis' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --ffi \
          --chain-id 100 \
          --broadcast \
          --legacy \
          --slow \
          --verify \
          -vvv
        ignore_error: true
      - task: sync-abis
  upgrade-gnosis:
    desc: Upgrade gnosis
    deps:
      - verify-storage
      - task: refresh-facets-gnosis
    cmds:
      - cmd: |
          forge script script/UpgradeCVMultichain.s.sol:UpgradeCVMultichain \
          --rpc-url ${RPC_URL_GNOSIS} \
          --sig "run(string)" 'gnosis' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --ffi \
          --chain-id 100 \
          --broadcast \
          --legacy \
          --gas-price 20000000000 \
          -vvv
      - task: verify-all-gnosis
  upgrade-atomic-diamond-gnosis:
    desc: Upgrade atomic diamond gnosis
    deps:
      - verify-storage
      - task: refresh-facets-gnosis
    cmds:
      - cmd: |
          forge script script/UpgradeAllDiamonds.s.sol:UpgradeAllDiamonds \
          --rpc-url ${RPC_URL_GNOSIS} \
          --sig "run(string)" 'gnosis' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --chain-id 100 \
          --broadcast \
          --ffi \
          --slow \
          --via-ir
  verify-blockscout-gnosis:
    desc: Verify blockscout gnosis
    cmds:
      - cmd: |
          forge script script/UpgradeCVMultichain.s.sol:UpgradeCVMultichain \
          --rpc-url ${RPC_URL_GNOSIS} \
          --sig "run(string)" 'gnosis' \
          --chain-id 100 \
          --account PK_DEPLOYER \
          --verifier blockscout \
          --verifier-url https://gnosis.blockscout.com/api/ \
          --ffi \
          --legacy \
          --verify \
          -vvv \
          --slow
        ignore_error: true
  verify-deploy-blockscout-gnosis:
    desc: Verify deploy blockscout gnosis
    cmds:
      - cmd: |
          forge script script/DeployCVMultiChain.s.sol:DeployCVMultiChain \
          --rpc-url ${RPC_URL_GNOSIS} \
          --sig "run(string)" 'gnosis' \
          --account PK_DEPLOYER \
          --verifier blockscout \
          --verifier-url https://gnosis.blockscout.com/api/ \
          --ffi \
          --chain-id 100 \
          --legacy \
          --verify \
          -vvv \
          --slow
        ignore_error: true
      - task: sync-abis
  deploy-passport-scorer-gnosis:
    desc: Deploy passport scorer gnosis
    cmds:
      - cmd: |
          forge script script/DeployPassportScorer.s.sol:DeployPassportScorer \
          --rpc-url ${RPC_URL_GNOSIS} \
          --sig "run(string)" 'gnosis' \
          --chain-id 100 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --verify \
          --broadcast \
          -vv
        ignore_error: true
      - task: sync-abis
  deploy-global-pause-controller-gnosis:
    desc: Deploy global pause controller gnosis
    cmds:
      - cmd: |
          forge script script/DeployGlobalPauseController.s.sol:DeployGlobalPauseController \
          --rpc-url ${RPC_URL_GNOSIS} \
          --sig "run(string)" 'gnosis' \
          --chain-id 100 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --verify \
          --broadcast \
          -vv
        ignore_error: true
      - task: sync-abis
  deploy-or-upgrade-global-pause-controller-gnosis:
    desc: Deploy or upgrade global pause controller gnosis
    cmds:
      - cmd: |
          forge script script/DeployGlobalPauseController.s.sol:DeployGlobalPauseController \
          --rpc-url ${RPC_URL_GNOSIS} \
          --sig "run(string)" 'gnosis' \
          --chain-id 100 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --broadcast \
          -vv \
          --verify
        ignore_error: true
      - task: sync-abis
  verify-global-pause-controller-gnosis:
    desc: Verify global pause controller gnosis
    cmds:
      - cmd: |
          bash ./scripts/verify-global-pause-controller.sh \
          --network gnosis \
          --rpc-url ${RPC_URL_GNOSIS} \
          --chain-id 100 \
          --etherscan-key ${ETHERSCAN_API_KEY} \
          --verifier-url https://gnosis.blockscout.com/api/
  deploy-or-upgrade-if-exists-safe-arbitrator-gnosis:
    desc: Deploy or upgrade if exists safe arbitrator gnosis
    cmds:
      - cmd: |
          forge script script/DeploySafeArbitrator.s.sol:DeploySafeArbitrator \
          --rpc-url ${RPC_URL_GNOSIS} \
          --sig "run(string)" 'gnosis' \
          --chain-id 100 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --verify \
          --broadcast \
          -vv
        ignore_error: true
      - task: sync-abis
  verify-tenderly-gnosis:
    desc: Verify tenderly gnosis
    cmds:
      - cmd: forge verify-contract \
        ignore_error: true
  upgrade-and-verify-gnosis:
    desc: Upgrade and verify gnosis
    deps:
      - verify-storage
      - task: refresh-facets-gnosis
    cmds:
      - task: upgrade-gnosis
      - task: verify-blockscout-gnosis
  deploy-and-verify-gnosis:
    desc: Deploy and verify gnosis
    deps:
      - deploy-gnosis
      - verify-deploy-blockscout-gnosis
    cmds: []
  change-passport-manager-gnosis:
    desc: Change passport manager gnosis
    cmds:
      - cmd: |
          forge script script/PassportScorerWriter.s.sol:PassportScorerWriter \
          --rpc-url ${RPC_URL_GNOSIS} \
          --sig "run(string)" 'gnosis' \
          --account PK_DEPLOYER \
          --ffi \
          --chain-id 100 \
          --broadcast \
          --legacy \
          -vvv
        ignore_error: true
  deploy-or-upgrade-good-dollar-sybil-gnosis:
    desc: Deploy or upgrade good dollar sybil gnosis
    cmds:
      - cmd: |
          forge script script/DeployGoodDollarSybil.s.sol:DeployGoodDollarSybil \
          --rpc-url ${RPC_URL_GNOSIS} \
          --sig "run(string)" 'gnosis' \
          --chain-id 100 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --verify \
          --broadcast \
          -vv
        ignore_error: true
      - task: sync-abis
  deploy-streaming-escrow-factory-gnosis:
    desc: Deploy streaming escrow factory gnosis (set on registry factory)
    cmds:
      - cmd: |
          RESUME_FLAG=""
          if [ "${RESUME:-false}" = "true" ]; then
            RESUME_FLAG="--resume"
          fi
          forge script script/DeployOrUpgradeStreamingEscrows.s.sol:DeployOrUpgradeStreamingEscrows \
          --rpc-url ${RPC_URL_GNOSIS} \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --sig "run(string,address,bool)" 'gnosis' ${I:-0x0000000000000000000000000000000000000000} {{default "false" .DEPLOY_IMPLEMENTATION_ONLY}} \
          --ffi \
          --broadcast \
          ${RESUME_FLAG} \
          --verify \
          --legacy \
          --chain-id 100 \
          -vvv
  verify-streaming-escrow-factory-gnosis:
    desc: Verify streaming escrow factory gnosis
    cmds:
      - cmd: |
          bash ./scripts/verify-streaming-escrow-factory.sh \
          --network gnosis \
          --rpc-url ${RPC_URL_GNOSIS} \
          --chain-id 100 \
          --etherscan-key ${ETHERSCAN_API_KEY} \
          --verifier-url https://gnosis.blockscout.com/api/
  upgrade-streaming-escrows-gnosis:
    desc: Deploy or upgrade streaming escrows on gnosis (networks.json managed in script, optional I implementation)
    cmds:
      - cmd: |
          forge script script/DeployOrUpgradeStreamingEscrows.s.sol:DeployOrUpgradeStreamingEscrows \
          --rpc-url ${RPC_URL_GNOSIS} \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --sig "run(string,address)" 'gnosis' ${I:-0x0000000000000000000000000000000000000000} \
          --ffi \
          --broadcast \
          --chain-id 100 \
          -vvv
  transfer-ownership-base:
    desc: Transfer ownership base
    cmds:
      - cmd: |
          forge script script/TransferOwnershipAllContracts.s.sol:TransferOwnershipAllContracts \
          --rpc-url ${RPC_URL_BASE} \
          --account PK_DEPLOYER \
          --sig "run(string)" 'base' \
          --chain-id 8453 \
          --broadcast \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --verify \
          -vvv
        ignore_error: true
  deploy-new-collateral-vault-template-base:
    desc: Deploy new collateral vault template base
    cmds:
      - cmd: |
          forge script script/DeployCollateralVaultTemplate.s.sol:DeployCollateralVaultTemplate \
          --rpc-url ${RPC_URL_BASE} \
          --sig "run(string)" 'base' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --ffi \
          --chain-id 8453 \
          --legacy \
          --broadcast \
          --verify \
          -vvv
        ignore_error: true
      - task: sync-abis
  deploy-base:
    desc: Deploy base
    cmds:
      - cmd: |
          forge script script/DeployCVMultiChain.s.sol:DeployCVMultiChain \
          --rpc-url ${RPC_URL_BASE} \
          --sig "run(string)" 'base' \
          --chain-id 8453 \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --ffi \
          --broadcast \
          --legacy \
          --verify \
          -vvv
        ignore_error: true
      - task: sync-abis
  upgrade-base:
    desc: Upgrade base
    deps:
      - verify-storage
      - task: refresh-facets-base
    cmds:
      - cmd: |
          forge script script/UpgradeCVMultichain.s.sol:UpgradeCVMultichain \
          --rpc-url ${RPC_URL_BASE} \
          --sig "run(string)" 'base' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --ffi \
          --chain-id 8453 \
          --broadcast \
          --legacy \
          --slow \
          -vvv
      - task: verify-all-base
  upgrade-atomic-diamond-base:
    desc: Upgrade atomic diamond base
    deps:
      - verify-storage
      - task: refresh-facets-base
    cmds:
      - cmd: |
          forge script script/UpgradeAllDiamonds.s.sol:UpgradeAllDiamonds \
          --rpc-url ${RPC_URL_BASE} \
          --sig "run(string)" 'base' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --chain-id 8453 \
          --broadcast \
          --ffi \
          --slow \
          --via-ir
  verify-blockscout-base:
    desc: Verify blockscout base
    cmds:
      - cmd: |
          forge script script/UpgradeCVMultichain.s.sol:UpgradeCVMultichain \
          --rpc-url ${RPC_URL_BASE} \
          --sig "run(string)" 'base' \
          --chain-id 8453 \
          --account PK_DEPLOYER \
          --verifier blockscout \
          --verifier-url https://base.blockscout.com/api/ \
          --ffi \
          --legacy \
          --verify \
          -vvv \
          --slow
        ignore_error: true
  verify-deploy-blockscout-base:
    desc: Verify deploy blockscout base
    cmds:
      - cmd: |
          forge script script/DeployCVMultiChain.s.sol:DeployCVMultiChain \
          --rpc-url ${RPC_URL_BASE} \
          --sig "run(string)" 'base' \
          --account PK_DEPLOYER \
          --verifier blockscout \
          --verifier-url https://base.blockscout.com/api/ \
          --ffi \
          --chain-id 8453 \
          --legacy \
          --verify \
          -vvv \
          --slow
        ignore_error: true
      - task: sync-abis
  deploy-and-verify-base:
    desc: Deploy and verify base
    deps:
      - deploy-base
      - verify-deploy-blockscout-base
    cmds: []
  upgrade-and-verify-base:
    desc: Upgrade and verify base
    deps:
      - verify-storage
      - task: refresh-facets-base
    cmds:
      - task: upgrade-base
      - task: verify-blockscout-base
  change-passport-manager-base:
    desc: Change passport manager base
    cmds:
      - cmd: |
          forge script script/PassportScorerWriter.s.sol:PassportScorerWriter \
          --rpc-url ${RPC_URL_BASE} \
          --sig "run(string)" 'base' \
          --chain-id 8453 \
          --account PK_DEPLOYER \
          --ffi \
          --broadcast \
          --legacy \
          -vvv
        ignore_error: true
  deploy-passport-scorer-base:
    desc: Deploy passport scorer base
    cmds:
      - cmd: |
          forge script script/DeployPassportScorer.s.sol:DeployPassportScorer \
          --rpc-url ${RPC_URL_BASE} \
          --sig "run(string)" 'base' \
          --chain-id 8453 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --verify \
          --broadcast \
          -vv
        ignore_error: true
      - task: sync-abis
  deploy-global-pause-controller-base:
    desc: Deploy global pause controller base
    cmds:
      - cmd: |
          forge script script/DeployGlobalPauseController.s.sol:DeployGlobalPauseController \
          --rpc-url ${RPC_URL_BASE} \
          --sig "run(string)" 'base' \
          --chain-id 8453 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --verify \
          --broadcast \
          -vv
        ignore_error: true
      - task: sync-abis
  deploy-or-upgrade-global-pause-controller-base:
    desc: Deploy or upgrade global pause controller base
    cmds:
      - cmd: |
          forge script script/DeployGlobalPauseController.s.sol:DeployGlobalPauseController \
          --rpc-url ${RPC_URL_BASE} \
          --sig "run(string)" 'base' \
          --chain-id 8453 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --broadcast \
          -vv \
          --verify
        ignore_error: true
      - task: sync-abis
  verify-global-pause-controller-base:
    desc: Verify global pause controller base
    cmds:
      - cmd: |
          bash ./scripts/verify-global-pause-controller.sh \
          --network base \
          --rpc-url ${RPC_URL_BASE} \
          --chain-id 8453 \
          --etherscan-key ${ETHERSCAN_API_KEY} \
          --verifier-url https://base.blockscout.com/api/
  deploy-or-upgrade-if-exists-safe-arbitrator-base:
    desc: Deploy or upgrade if exists safe arbitrator base
    cmds:
      - cmd: |
          forge script script/DeploySafeArbitrator.s.sol:DeploySafeArbitrator \
          --rpc-url ${RPC_URL_BASE} \
          --sig "run(string)" 'base' \
          --chain-id 8453 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --verify \
          --broadcast \
          -vv
        ignore_error: true
      - task: sync-abis
  deploy-proxy-owner-base:
    desc: Deploy proxy owner base
    cmds:
      - cmd: |
          forge script script/DeployProxyOwner.s.sol:DeployProxyOwner \
          --rpc-url ${RPC_URL_BASE} \
          --sig "run(string)" 'base' \
          --chain-id 8453 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --verify \
          --broadcast \
          -vv
        ignore_error: true
      - task: sync-abis
  upgrade-proxy-owner-ethsep:
    desc: Upgrade proxy owner ethsepolia
    cmds:
      - cmd: |
          forge script script/UpgradeProxyOwner.s.sol:UpgradeProxyOwner \
          --rpc-url ${RPC_URL_SEP_TESTNET} \
          --sig "run(string,address,bool)" 'ethsepolia' ${I:-0x0000000000000000000000000000000000000000} {{default "false" .DEPLOY_IMPLEMENTATION_ONLY}} \
          --chain-id 11155111 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --ffi \
          --verify \
          --broadcast \
          -vv
  upgrade-proxy-owner-arbsep:
    desc: Upgrade proxy owner arbsepolia
    cmds:
      - cmd: |
          forge script script/UpgradeProxyOwner.s.sol:UpgradeProxyOwner \
          --rpc-url ${RPC_URL_ARB_TESTNET} \
          --sig "run(string,address,bool)" 'arbsepolia' ${I:-0x0000000000000000000000000000000000000000} {{default "false" .DEPLOY_IMPLEMENTATION_ONLY}} \
          --chain-id 421614 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --ffi \
          --verify \
          --broadcast \
          -vv
  upgrade-proxy-owner-opsep:
    desc: Upgrade proxy owner opsepolia
    cmds:
      - cmd: |
          forge script script/UpgradeProxyOwner.s.sol:UpgradeProxyOwner \
          --rpc-url ${RPC_URL_OP_TESTNET} \
          --sig "run(string,address,bool)" 'opsepolia' ${I:-0x0000000000000000000000000000000000000000} {{default "false" .DEPLOY_IMPLEMENTATION_ONLY}} \
          --chain-id 11155420 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --ffi \
          --verify \
          --broadcast \
          -vv
  upgrade-proxy-owner-arbitrum:
    desc: Upgrade proxy owner arbitrum
    cmds:
      - cmd: |
          forge script script/UpgradeProxyOwner.s.sol:UpgradeProxyOwner \
          --rpc-url ${RPC_URL_ARB} \
          --sig "run(string,address,bool)" 'arbitrum' ${I:-0x0000000000000000000000000000000000000000} {{default "false" .DEPLOY_IMPLEMENTATION_ONLY}} \
          --chain-id 42161 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --ffi \
          --verify \
          --broadcast \
          --legacy \
          -vv
  upgrade-proxy-owner-optimism:
    desc: Upgrade proxy owner optimism
    cmds:
      - cmd: |
          forge script script/UpgradeProxyOwner.s.sol:UpgradeProxyOwner \
          --rpc-url ${RPC_URL_OPT} \
          --sig "run(string,address,bool)" 'optimism' ${I:-0x0000000000000000000000000000000000000000} {{default "false" .DEPLOY_IMPLEMENTATION_ONLY}} \
          --chain-id 10 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --ffi \
          --verify \
          --broadcast \
          --legacy \
          -vv
  upgrade-proxy-owner-polygon:
    desc: Upgrade proxy owner polygon
    cmds:
      - cmd: |
          forge script script/UpgradeProxyOwner.s.sol:UpgradeProxyOwner \
          --rpc-url ${RPC_URL_POLYGON} \
          --sig "run(string,address,bool)" 'polygon' ${I:-0x0000000000000000000000000000000000000000} {{default "false" .DEPLOY_IMPLEMENTATION_ONLY}} \
          --chain-id 137 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --ffi \
          --verify \
          --broadcast \
          --legacy \
          -vv
  upgrade-proxy-owner-gnosis:
    desc: Upgrade proxy owner gnosis
    cmds:
      - cmd: |
          forge script script/UpgradeProxyOwner.s.sol:UpgradeProxyOwner \
          --rpc-url ${RPC_URL_GNOSIS} \
          --sig "run(string,address,bool)" 'gnosis' ${I:-0x0000000000000000000000000000000000000000} {{default "false" .DEPLOY_IMPLEMENTATION_ONLY}} \
          --chain-id 100 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --ffi \
          --verify \
          --broadcast \
          --legacy \
          -vv
  upgrade-proxy-owner-base:
    desc: Upgrade proxy owner base
    cmds:
      - cmd: |
          forge script script/UpgradeProxyOwner.s.sol:UpgradeProxyOwner \
          --rpc-url ${RPC_URL_BASE} \
          --sig "run(string,address,bool)" 'base' ${I:-0x0000000000000000000000000000000000000000} {{default "false" .DEPLOY_IMPLEMENTATION_ONLY}} \
          --chain-id 8453 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --ffi \
          --verify \
          --broadcast \
          --legacy \
          -vv
  upgrade-proxy-owner-celo:
    desc: Upgrade proxy owner celo
    cmds:
      - cmd: |
          forge script script/UpgradeProxyOwner.s.sol:UpgradeProxyOwner \
          --rpc-url ${RPC_URL_CELO} \
          --sig "run(string,address,bool)" 'celo' ${I:-0x0000000000000000000000000000000000000000} {{default "false" .DEPLOY_IMPLEMENTATION_ONLY}} \
          --chain-id 42220 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --ffi \
          --verify \
          --broadcast \
          --legacy \
          -vv
  upgrade-proxy-owner-mainnets:
    desc: Upgrade proxy owner on all mainnets
    cmds:
      - echo "Upgrading proxy owner on arbitrum"
      - task: upgrade-proxy-owner-arbitrum
      - echo "Upgrading proxy owner on optimism"
      - task: upgrade-proxy-owner-optimism
      - echo "Upgrading proxy owner on polygon"
      - task: upgrade-proxy-owner-polygon
      - echo "Upgrading proxy owner on gnosis"
      - task: upgrade-proxy-owner-gnosis
      - echo "Upgrading proxy owner on base"
      - task: upgrade-proxy-owner-base
      - echo "Upgrading proxy owner on celo"
      - task: upgrade-proxy-owner-celo
  deploy-proxy-owner-implementation-mainnets:
    desc: Deploy ProxyOwner implementation only on all mainnets and snapshot IMPLEMENTATIONS.PROXY_OWNER
    cmds:
      - echo "Deploying ProxyOwner implementation on arbitrum"
      - task: upgrade-proxy-owner-arbitrum
        vars:
          DEPLOY_IMPLEMENTATION_ONLY: "true"
      - echo "Deploying ProxyOwner implementation on optimism"
      - task: upgrade-proxy-owner-optimism
        vars:
          DEPLOY_IMPLEMENTATION_ONLY: "true"
      - echo "Deploying ProxyOwner implementation on polygon"
      - task: upgrade-proxy-owner-polygon
        vars:
          DEPLOY_IMPLEMENTATION_ONLY: "true"
      - echo "Deploying ProxyOwner implementation on gnosis"
      - task: upgrade-proxy-owner-gnosis
        vars:
          DEPLOY_IMPLEMENTATION_ONLY: "true"
      - echo "Deploying ProxyOwner implementation on base"
      - task: upgrade-proxy-owner-base
        vars:
          DEPLOY_IMPLEMENTATION_ONLY: "true"
      - echo "Deploying ProxyOwner implementation on celo"
      - task: upgrade-proxy-owner-celo
        vars:
          DEPLOY_IMPLEMENTATION_ONLY: "true"
  deploy-or-upgrade-good-dollar-sybil-base:
    desc: Deploy or upgrade good dollar sybil base
    cmds:
      - cmd: |
          forge script script/DeployGoodDollarSybil.s.sol:DeployGoodDollarSybil \
          --rpc-url ${RPC_URL_BASE} \
          --sig "run(string)" 'base' \
          --chain-id 8453 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --verify \
          --broadcast \
          -vv
        ignore_error: true
      - task: sync-abis
  deploy-streaming-escrow-factory-base:
    desc: Deploy streaming escrow factory base (set on registry factory)
    cmds:
      - cmd: |
          RESUME_FLAG=""
          if [ "${RESUME:-false}" = "true" ]; then
            RESUME_FLAG="--resume"
          fi
          forge script script/DeployOrUpgradeStreamingEscrows.s.sol:DeployOrUpgradeStreamingEscrows \
          --rpc-url ${RPC_URL_BASE} \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --sig "run(string,address)" 'base' ${I:-0x0000000000000000000000000000000000000000} \
          --ffi \
          --broadcast \
          ${RESUME_FLAG} \
          --verify \
          --legacy \
          --chain-id 8453 \
          -vvv
  verify-streaming-escrow-factory-base:
    desc: Verify streaming escrow factory base
    cmds:
      - cmd: |
          bash ./scripts/verify-streaming-escrow-factory.sh \
          --network base \
          --rpc-url ${RPC_URL_BASE} \
          --chain-id 8453 \
          --etherscan-key ${ETHERSCAN_API_KEY} \
          --verifier-url https://base.blockscout.com/api/
  upgrade-streaming-escrows-base:
    desc: Deploy or upgrade streaming escrows on base (networks.json managed in script, optional I implementation)
    cmds:
      - cmd: |
          forge script script/DeployOrUpgradeStreamingEscrows.s.sol:DeployOrUpgradeStreamingEscrows \
          --rpc-url ${RPC_URL_BASE} \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --sig "run(string,address)" 'base' ${I:-0x0000000000000000000000000000000000000000} \
          --ffi \
          --broadcast \
          --chain-id 8453 \
          -vvv
  transfer-ownership-celo:
    desc: Transfer ownership celo
    cmds:
      - cmd: |
          forge script script/TransferOwnershipAllContracts.s.sol:TransferOwnershipAllContracts \
          --rpc-url ${RPC_URL_CELO} \
          --account PK_DEPLOYER \
          --sig "run(string)" 'celo' \
          --chain-id 42220 \
          --broadcast \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --verify \
          -vvv
        ignore_error: true
  deploy-new-collateral-vault-template-celo:
    desc: Deploy new collateral vault template celo
    cmds:
      - cmd: |
          forge script script/DeployCollateralVaultTemplate.s.sol:DeployCollateralVaultTemplate \
          --rpc-url ${RPC_URL_CELO} \
          --sig "run(string)" 'celo' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --ffi \
          --chain-id 42220 \
          --legacy \
          --broadcast \
          --verify \
          -vvv
        ignore_error: true
      - task: sync-abis
  deploy-celo:
    desc: Deploy celo
    cmds:
      - cmd: |
          forge script script/DeployCVMultiChain.s.sol:DeployCVMultiChain \
          --rpc-url ${RPC_URL_CELO} \
          --sig "run(string)" 'celo' \
          --chain-id 42220 \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --ffi \
          --broadcast \
          --legacy \
          --verify \
          -vvv
        ignore_error: true
      - task: sync-abis
  upgrade-celo:
    desc: Upgrade celo
    deps:
      - verify-storage
      - task: refresh-facets-celo
    cmds:
      - cmd: |
          forge script script/UpgradeCVMultichain.s.sol:UpgradeCVMultichain \
          --rpc-url ${RPC_URL_CELO} \
          --sig "run(string)" 'celo' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --ffi \
          --chain-id 42220 \
          --broadcast \
          --legacy \
          --slow \
          -vvv
      - task: verify-all-celo
  upgrade-atomic-diamond-celo:
    desc: Upgrade atomic diamond celo
    deps:
      - verify-storage
      - task: refresh-facets-celo
    cmds:
      - cmd: |
          forge script script/UpgradeAllDiamonds.s.sol:UpgradeAllDiamonds \
          --rpc-url ${RPC_URL_CELO} \
          --sig "run(string)" 'celo' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --chain-id 42220 \
          --broadcast \
          --ffi \
          --slow \
          --via-ir
  verify-blockscout-celo:
    desc: Verify blockscout celo
    cmds:
      - cmd: |
          forge script script/UpgradeCVMultichain.s.sol:UpgradeCVMultichain \
          --rpc-url ${RPC_URL_CELO} \
          --sig "run(string)" 'celo' \
          --chain-id 42220 \
          --account PK_DEPLOYER \
          --verifier blockscout \
          --verifier-url https://celo.blockscout.com/api/ \
          --ffi \
          --legacy \
          --verify \
          -vvv \
          --slow
        ignore_error: true
  verify-deploy-blockscout-celo:
    desc: Verify deploy blockscout celo
    cmds:
      - cmd: |
          forge script script/DeployCVMultiChain.s.sol:DeployCVMultiChain \
          --rpc-url ${RPC_URL_CELO} \
          --sig "run(string)" 'celo' \
          --account PK_DEPLOYER \
          --verifier blockscout \
          --verifier-url https://celo.blockscout.com/api/ \
          --ffi \
          --chain-id 42220 \
          --legacy \
          --verify \
          -vvv \
          --slow
        ignore_error: true
      - task: sync-abis
  deploy-and-verify-celo:
    desc: Deploy and verify celo
    deps:
      - deploy-celo
      - verify-deploy-blockscout-celo
    cmds: []
  upgrade-and-verify-celo:
    desc: Upgrade and verify celo
    deps:
      - verify-storage
      - task: refresh-facets-celo
    cmds:
      - task: upgrade-celo
      - task: verify-blockscout-celo

  deploy-mainnet:
    desc: Deploy mainnet
    cmds:
      - cmd: |
          forge script script/DeployCVMultiChain.s.sol:DeployCVMultiChain \
          --rpc-url ${RPC_URL_MAINNET} \
          --sig "run(string)" 'mainnet' \
          --chain-id 1 \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --ffi \
          --broadcast \
          --verify \
          -vvv
        ignore_error: true
      - task: sync-abis

  upgrade-mainnet:
    desc: Upgrade mainnet
    deps:
      - verify-storage
      - task: refresh-facets-mainnet
    cmds:
      - cmd: |
          forge script script/UpgradeCVMultichain.s.sol:UpgradeCVMultichain \
          --rpc-url ${RPC_URL_MAINNET} \
          --sig "run(string)" 'mainnet' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --ffi \
          --chain-id 1 \
          --broadcast \
          -vvv
      - task: verify-all-mainnet

  upgrade-atomic-diamond-mainnet:
    desc: Upgrade atomic diamond mainnet
    deps:
      - verify-storage
      - task: refresh-facets-mainnet
    cmds:
      - cmd: |
          forge script script/UpgradeAllDiamonds.s.sol:UpgradeAllDiamonds \
          --rpc-url ${RPC_URL_MAINNET} \
          --sig "run(string)" 'mainnet' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --chain-id 1 \
          --broadcast \
          --ffi \
          --slow \
          --via-ir

  verify-etherscan-mainnet:
    desc: Verify contracts on Etherscan mainnet
    cmds:
      - cmd: |
          forge script script/UpgradeCVMultichain.s.sol:UpgradeCVMultichain \
          --rpc-url ${RPC_URL_MAINNET} \
          --sig "run(string)" 'mainnet' \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --ffi \
          --chain-id 1 \
          --verify \
          -vvv \
          --slow
        ignore_error: true

  upgrade-and-verify-mainnet:
    desc: Upgrade and verify mainnet
    deps:
      - verify-storage
      - task: refresh-facets-mainnet
    cmds:
      - task: upgrade-mainnet
      - task: verify-etherscan-mainnet

  upgrade-all-mainnets:
    desc: Upgrade arbitrum, optimism, polygon, gnosis, base, celo in parallel, then verify global deployments
    deps:
      - verify-storage
    cmds:
      - cmd: |
          set +e
          for network in arbitrum optimism polygon gnosis base celo; do
            if ! task "refresh-facets-${network}"; then
              echo "❌ refresh ${network}: failed"
              exit 1
            fi
          done

          declare -a pids
          declare -a names
          failed=0

          (
            forge script script/UpgradeCVMultichain.s.sol:UpgradeCVMultichain \
            --rpc-url ${RPC_URL_ARB} \
            --sig "run(string)" 'arbitrum' \
            --account PK_DEPLOYER \
            --etherscan-api-key ${ETHERSCAN_API_KEY} \
            --ffi \
            --chain-id 42161 \
            --broadcast \
            --slow \
            -vvv
          ) & pids+=($!); names+=("arbitrum")

          (
            forge script script/UpgradeCVMultichain.s.sol:UpgradeCVMultichain \
            --rpc-url ${RPC_URL_OPT} \
            --sig "run(string)" 'optimism' \
            --account PK_DEPLOYER \
            --etherscan-api-key ${ETHERSCAN_API_KEY} \
            --ffi \
            --chain-id 10 \
            --broadcast \
            --legacy \
            -vvv
          ) & pids+=($!); names+=("optimism")

          (
            forge script script/UpgradeCVMultichain.s.sol:UpgradeCVMultichain \
            --rpc-url ${RPC_URL_POLYGON} \
            --sig "run(string)" 'polygon' \
            --account PK_DEPLOYER \
            --etherscan-api-key ${ETHERSCAN_API_KEY} \
            --ffi \
            --chain-id 137 \
            --broadcast \
            --legacy \
            --slow \
            -vvv
          ) & pids+=($!); names+=("polygon")

          (
            forge script script/UpgradeCVMultichain.s.sol:UpgradeCVMultichain \
            --rpc-url ${RPC_URL_GNOSIS} \
            --sig "run(string)" 'gnosis' \
            --account PK_DEPLOYER \
            --etherscan-api-key ${ETHERSCAN_API_KEY} \
            --ffi \
            --chain-id 100 \
            --broadcast \
            --legacy \
            --gas-price 20000000000 \
            -vvv
          ) & pids+=($!); names+=("gnosis")

          (
            forge script script/UpgradeCVMultichain.s.sol:UpgradeCVMultichain \
            --rpc-url ${RPC_URL_BASE} \
            --sig "run(string)" 'base' \
            --account PK_DEPLOYER \
            --etherscan-api-key ${ETHERSCAN_API_KEY} \
            --ffi \
            --chain-id 8453 \
            --broadcast \
            --legacy \
            --slow \
            -vvv
          ) & pids+=($!); names+=("base")

          (
            forge script script/UpgradeCVMultichain.s.sol:UpgradeCVMultichain \
            --rpc-url ${RPC_URL_CELO} \
            --sig "run(string)" 'celo' \
            --account PK_DEPLOYER \
            --etherscan-api-key ${ETHERSCAN_API_KEY} \
            --ffi \
            --chain-id 42220 \
            --broadcast \
            --legacy \
            --slow \
            -vvv
          ) & pids+=($!); names+=("celo")

          for i in "${!pids[@]}"; do
            if wait "${pids[$i]}"; then
              echo "✅ ${names[$i]}: success"
            else
              echo "❌ ${names[$i]}: failed"
              failed=1
            fi
          done

          if task verify-all-mainnets; then
            echo "✅ all verification: success"
          else
            echo "❌ all verification: failed"
            failed=1
          fi

          if [ "$failed" -ne 0 ]; then
            echo "One or more networks failed."
          else
            echo "All networks succeeded."
          fi
          exit 0

  deploy-or-upgrade-global-pause-controller-all-mainnets:
    desc: Deploy or upgrade global pause controller on arbitrum, optimism, polygon, gnosis, base, celo
    cmds:
      - cmd: |
          set +e
          failed=0

          for network in arbitrum optimism polygon gnosis base celo; do
            if task "deploy-or-upgrade-global-pause-controller-${network}"; then
              echo "✅ ${network}: success"
            else
              echo "❌ ${network}: failed"
              failed=1
            fi
          done

          if [ "$failed" -ne 0 ]; then
            echo "One or more networks failed."
          else
            echo "All networks succeeded."
          fi
          exit 0

  deploy-streaming-escrow-factory-all-mainnets:
    desc: Deploy streaming escrow factory on arbitrum, optimism, polygon, gnosis, base, celo
    cmds:
      - cmd: |
          set +e
          failed=0

          for network in arbitrum optimism polygon gnosis base celo; do
            if task "deploy-streaming-escrow-factory-${network}"; then
              echo "✅ ${network}: success"
            else
              echo "❌ ${network}: failed"
              failed=1
            fi
          done

          if [ "$failed" -ne 0 ]; then
            echo "One or more networks failed."
          else
            echo "All networks succeeded."
          fi
          exit 0

  verify-global-pause-controller-all-mainnets:
    desc: Verify global pause controller on arbitrum, optimism, polygon, gnosis, base, celo
    cmds:
      - cmd: |
          set +e
          failed=0

          for network in arbitrum optimism polygon gnosis base celo; do
            if task "verify-global-pause-controller-${network}"; then
              echo "✅ ${network}: success"
            else
              echo "❌ ${network}: failed"
              failed=1
            fi
          done

          if [ "$failed" -ne 0 ]; then
            echo "One or more networks failed."
          else
            echo "All networks succeeded."
          fi
          exit 0

  verify-streaming-escrow-factory-all-mainnets:
    desc: Verify streaming escrow factory on arbitrum, optimism, polygon, gnosis, base, celo
    cmds:
      - cmd: |
          set +e
          failed=0

          for network in arbitrum optimism polygon gnosis base celo; do
            if task "verify-streaming-escrow-factory-${network}"; then
              echo "✅ ${network}: success"
            else
              echo "❌ ${network}: failed"
              failed=1
            fi
          done

          if [ "$failed" -ne 0 ]; then
            echo "One or more networks failed."
          else
            echo "All networks succeeded."
          fi
          exit 0

  verify-global-deployments-all-mainnets:
    desc: Verify deployments on arbitrum, optimism, polygon, gnosis, base, celo; pass SCOPE=global|state|pause|escrow|factory|communities|strategies|facets|all
    cmds:
      - cmd: |
          bash ./scripts/verify-all-deployments.sh --scope "${SCOPE:-global}"

  verify-all-mainnets:
    desc: Verify all deployments on arbitrum, optimism, polygon, gnosis, base, celo; pass SCOPE=global|state|pause|escrow|factory|communities|strategies|facets|all
    cmds:
      - cmd: |
          bash ./scripts/verify-all-deployments.sh --scope "${SCOPE:-all}"

  pause-all-networks-with-pause-controller:
    desc: Pause factory, communities and strategies on every network with ENVS.PAUSE_CONTROLLER set
    cmds:
      - cmd: |
          set +e

          DURATION_SECONDS=${DURATION_SECONDS:-3600}
          if [ "${DURATION_SECONDS}" -le 0 ]; then
            echo "[FAIL] DURATION_SECONDS must be > 0"
            exit 1
          fi

          NETWORKS=$(jq -r '.networks[] | select(.ENVS.PAUSE_CONTROLLER? != null and (.ENVS.PAUSE_CONTROLLER | ascii_downcase) != "0x0000000000000000000000000000000000000000") | .name' config/networks.json)
          if [ -z "${NETWORKS}" ]; then
            echo "[INFO] No network has ENVS.PAUSE_CONTROLLER set"
            exit 0
          fi

          failed=0
          failed_networks=""
          succeeded_networks=""
          declare -a pids
          declare -a names

          for network in ${NETWORKS}; do
            rpc=""
            chain_id=""

            case "${network}" in
              ethsepolia) rpc="${RPC_URL_SEP_TESTNET}"; chain_id="11155111" ;;
              arbsepolia) rpc="${RPC_URL_ARB_TESTNET}"; chain_id="421614" ;;
              opsepolia) rpc="${RPC_URL_OP_TESTNET}"; chain_id="11155420" ;;
              arbitrum) rpc="${RPC_URL_ARB}"; chain_id="42161" ;;
              optimism) rpc="${RPC_URL_OPT}"; chain_id="10" ;;
              polygon) rpc="${RPC_URL_POLYGON}"; chain_id="137" ;;
              gnosis) rpc="${RPC_URL_GNOSIS}"; chain_id="100" ;;
              base) rpc="${RPC_URL_BASE}"; chain_id="8453" ;;
              celo) rpc="${RPC_URL_CELO}"; chain_id="42220" ;;
              mainnet) rpc="${RPC_URL_MAINNET}"; chain_id="1" ;;
              *)
                echo "[FAIL] ${network}: unknown network mapping"
                failed=1
                failed_networks="${failed_networks} ${network}"
                continue
                ;;
            esac

            if [ -z "${rpc}" ]; then
              echo "[FAIL] ${network}: missing RPC env var for network"
              failed=1
              failed_networks="${failed_networks} ${network}"
              continue
            fi

            echo "[INFO] ${network}: pausing targets for ${DURATION_SECONDS}s"
            forge script script/PauseAllByController.s.sol:PauseAllByController \
              --rpc-url "${rpc}" \
              --account PK_DEPLOYER \
              --sig "run(string,uint256)" "${network}" "${DURATION_SECONDS}" \
              --chain-id "${chain_id}" \
              --broadcast \
              --slow \
              -vv &
            pids+=($!)
            names+=("${network}")
          done

          for i in "${!pids[@]}"; do
            if wait "${pids[$i]}"; then
              echo "[OK] ${names[$i]}"
              succeeded_networks="${succeeded_networks} ${names[$i]}"
            else
              echo "[FAIL] ${names[$i]}"
              failed=1
              failed_networks="${failed_networks} ${names[$i]}"
            fi
          done

          if [ "${failed}" -ne 0 ]; then
            echo "[SUMMARY] pause run completed with failures"
            echo "[SUMMARY] succeeded:${succeeded_networks}"
            echo "[SUMMARY] failed:${failed_networks}"
            exit 1
          fi

          echo "[SUMMARY] pause run completed successfully on all eligible networks"
          echo "[SUMMARY] succeeded:${succeeded_networks}"
  unpause-all-networks-with-pause-controller:
    desc: Unpause factory, communities and strategies on every network with ENVS.PAUSE_CONTROLLER set
    cmds:
      - cmd: |
          set +e

          NETWORKS=$(jq -r '.networks[] | select(.ENVS.PAUSE_CONTROLLER? != null and (.ENVS.PAUSE_CONTROLLER | ascii_downcase) != "0x0000000000000000000000000000000000000000") | .name' config/networks.json)
          if [ -z "${NETWORKS}" ]; then
            echo "[INFO] No network has ENVS.PAUSE_CONTROLLER set"
            exit 0
          fi

          failed=0
          failed_networks=""
          succeeded_networks=""

          for network in ${NETWORKS}; do
            rpc=""
            chain_id=""

            case "${network}" in
              ethsepolia) rpc="${RPC_URL_SEP_TESTNET}"; chain_id="11155111" ;;
              arbsepolia) rpc="${RPC_URL_ARB_TESTNET}"; chain_id="421614" ;;
              opsepolia) rpc="${RPC_URL_OP_TESTNET}"; chain_id="11155420" ;;
              arbitrum) rpc="${RPC_URL_ARB}"; chain_id="42161" ;;
              optimism) rpc="${RPC_URL_OPT}"; chain_id="10" ;;
              polygon) rpc="${RPC_URL_POLYGON}"; chain_id="137" ;;
              gnosis) rpc="${RPC_URL_GNOSIS}"; chain_id="100" ;;
              base) rpc="${RPC_URL_BASE}"; chain_id="8453" ;;
              celo) rpc="${RPC_URL_CELO}"; chain_id="42220" ;;
              mainnet) rpc="${RPC_URL_MAINNET}"; chain_id="1" ;;
              *)
                echo "[FAIL] ${network}: unknown network mapping"
                failed=1
                failed_networks="${failed_networks} ${network}"
                continue
                ;;
            esac

            if [ -z "${rpc}" ]; then
              echo "[FAIL] ${network}: missing RPC env var for network"
              failed=1
              failed_networks="${failed_networks} ${network}"
              continue
            fi

            echo "[INFO] ${network}: unpausing targets"
            if forge script script/UnpauseAllByController.s.sol:UnpauseAllByController \
              --rpc-url "${rpc}" \
              --account PK_DEPLOYER \
              --sig "run(string)" "${network}" \
              --chain-id "${chain_id}" \
              --broadcast \
              -vv; then
              echo "[OK] ${network}"
              succeeded_networks="${succeeded_networks} ${network}"
            else
              echo "[FAIL] ${network}"
              failed=1
              failed_networks="${failed_networks} ${network}"
            fi
          done

          if [ "${failed}" -ne 0 ]; then
            echo "[SUMMARY] unpause run completed with failures"
            echo "[SUMMARY] succeeded:${succeeded_networks}"
            echo "[SUMMARY] failed:${failed_networks}"
            exit 1
          fi

          echo "[SUMMARY] unpause run completed successfully on all eligible networks"
          echo "[SUMMARY] succeeded:${succeeded_networks}"
  submit-safe-payloads:
    desc: Submit Safe Transaction Builder payloads to the Safe Transaction Service
    cmds:
      - cmd: |
          python scripts/submit_safe_payloads.py \
          --safe 0x9a17De1f0caD0c592F656410997E4B685d339029 \
          --keystore ~/.foundry/keystores/pkMetamask
  submit-safe-payloads-test:
    desc: Submit Safe Transaction Builder payloads to the Safe Transaction Service
    cmds:
      - cmd: |
          python scripts/submit_safe_payloads.py \
          --safe 0x9a17De1f0caD0c592F656410997E4B685d339029 \
          --keystore ~/.foundry/keystores/pkMetamask \
          --test
  submit-safe-payloads-force:
    desc: Submit Safe Transaction Builder payloads to the Safe Transaction Service
    cmds:
      - cmd: |
          python scripts/submit_safe_payloads.py \
          --safe 0x9a17De1f0caD0c592F656410997E4B685d339029 \
          --keystore ~/.foundry/keystores/pkMetamask \
          --force
  submit-safe-payloads-test-force:
    desc: Submit Safe Transaction Builder payloads to the Safe Transaction Service
    cmds:
      - cmd: |
          python scripts/submit_safe_payloads.py \
          --safe 0x9a17De1f0caD0c592F656410997E4B685d339029 \
          --keystore ~/.foundry/keystores/pkMetamask \
          --test \
          --force
  submit-safe-payloads-arbitrum:
    desc: Submit Safe payload for arbitrum
    cmds:
      - cmd: |
          python scripts/submit_safe_payloads.py \
          --safe 0x9a17De1f0caD0c592F656410997E4B685d339029 \
          --keystore ~/.foundry/keystores/pkMetamask \
          --chains arbitrum
  submit-safe-payloads-optimism:
    desc: Submit Safe payload for optimism
    cmds:
      - cmd: |
          python scripts/submit_safe_payloads.py \
          --safe 0x9a17De1f0caD0c592F656410997E4B685d339029 \
          --keystore ~/.foundry/keystores/pkMetamask \
          --chains optimism
  submit-safe-payloads-polygon:
    desc: Submit Safe payload for polygon
    cmds:
      - cmd: |
          python scripts/submit_safe_payloads.py \
          --safe 0x9a17De1f0caD0c592F656410997E4B685d339029 \
          --keystore ~/.foundry/keystores/pkMetamask \
          --chains polygon
  submit-safe-payloads-gnosis:
    desc: Submit Safe payload for gnosis
    cmds:
      - cmd: |
          python scripts/submit_safe_payloads.py \
          --safe 0x9a17De1f0caD0c592F656410997E4B685d339029 \
          --keystore ~/.foundry/keystores/pkMetamask \
          --chains gnosis
  submit-safe-payloads-base:
    desc: Submit Safe payload for base
    cmds:
      - cmd: |
          python scripts/submit_safe_payloads.py \
          --safe 0x9a17De1f0caD0c592F656410997E4B685d339029 \
          --keystore ~/.foundry/keystores/pkMetamask \
          --chains base
  submit-safe-payloads-celo:
    desc: Submit Safe payload for celo
    cmds:
      - cmd: |
          python scripts/submit_safe_payloads.py \
          --safe 0x9a17De1f0caD0c592F656410997E4B685d339029 \
          --keystore ~/.foundry/keystores/pkMetamask \
          --chains celo
  clear-safe-queue:
    desc: Clear queued Safe transactions via Safe Transaction Service
    cmds:
      - cmd: |
          python scripts/clear_safe_queue.py \
          --safe 0x9a17De1f0caD0c592F656410997E4B685d339029 \
          --keystore ~/.foundry/keystores/pkMetamask
  clear-safe-queue-test:
    desc: Clear queued Safe transactions in sandbox Safe
    cmds:
      - cmd: |
          python scripts/clear_safe_queue.py \
          --safe 0x9a17De1f0caD0c592F656410997E4B685d339029 \
          --keystore ~/.foundry/keystores/pkMetamask \
          --test
  list-safe-queue:
    desc: List queued Safe transactions and proposers
    cmds:
      - cmd: |
          python scripts/clear_safe_queue.py \
          --safe 0x9a17De1f0caD0c592F656410997E4B685d339029 \
          --keystore ~/.foundry/keystores/pkMetamask \
          --list
  change-passport-manager-celo:
    desc: Change passport manager celo
    cmds:
      - cmd: |
          forge script script/PassportScorerWriter.s.sol:PassportScorerWriter \
          --rpc-url ${RPC_URL_CELO} \
          --sig "run(string)" 'celo' \
          --chain-id 42220 \
          --account PK_DEPLOYER \
          --ffi \
          --broadcast \
          --legacy \
          -vvv
        ignore_error: true
  deploy-passport-scorer-celo:
    desc: Deploy passport scorer celo
    cmds:
      - cmd: |
          forge script script/DeployPassportScorer.s.sol:DeployPassportScorer \
          --rpc-url ${RPC_URL_CELO} \
          --sig "run(string)" 'celo' \
          --chain-id 42220 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --verify \
          --broadcast \
          -vv
        ignore_error: true
      - task: sync-abis
  deploy-global-pause-controller-celo:
    desc: Deploy global pause controller celo
    cmds:
      - cmd: |
          forge script script/DeployGlobalPauseController.s.sol:DeployGlobalPauseController \
          --rpc-url ${RPC_URL_CELO} \
          --sig "run(string)" 'celo' \
          --chain-id 42220 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --verify \
          --broadcast \
          -vv
        ignore_error: true
      - task: sync-abis
  deploy-or-upgrade-global-pause-controller-celo:
    desc: Deploy or upgrade global pause controller celo
    cmds:
      - cmd: |
          forge script script/DeployGlobalPauseController.s.sol:DeployGlobalPauseController \
          --rpc-url ${RPC_URL_CELO} \
          --sig "run(string)" 'celo' \
          --chain-id 42220 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --broadcast \
          -vv \
          --verify
        ignore_error: true
      - task: sync-abis
  verify-global-pause-controller-celo:
    desc: Verify global pause controller celo
    cmds:
      - cmd: |
          bash ./scripts/verify-global-pause-controller.sh \
          --network celo \
          --rpc-url ${RPC_URL_CELO} \
          --chain-id 42220 \
          --etherscan-key ${ETHERSCAN_API_KEY} \
          --verifier-url https://celo.blockscout.com/api/
  verify-blockscout-passport-scorer-celo:
    desc: Verify blockscout passport scorer celo
    cmds:
      - cmd: |
          forge script script/DeployPassportScorer.s.sol:DeployPassportScorer \
          --rpc-url ${RPC_URL_CELO} \
          --sig "run(string)" 'celo' \
          --chain-id 42220 \
          --verifier blockscout \
          --verifier-url https://celo.blockscout.com/api/ \
          --account PK_DEPLOYER \
          --verify \
          -vv
        ignore_error: true
      - task: sync-abis
  deploy-or-upgrade-if-exists-safe-arbitrator-celo:
    desc: Deploy or upgrade if exists safe arbitrator celo
    cmds:
      - cmd: |
          forge script script/DeploySafeArbitrator.s.sol:DeploySafeArbitrator \
          --rpc-url ${RPC_URL_CELO} \
          --sig "run(string)" 'celo' \
          --chain-id 42220 \
          --verifier blockscout \
          --verifier-url https://celo.blockscout.com/api/ \
          --account PK_DEPLOYER \
          --verify \
          -vv \
        ignore_error: true
      - task: sync-abis
  verify-blockscout-proxy-owner-celo:
    desc: Verify blockscout proxy owner celo
    cmds:
      - cmd: |
          forge script script/DeployProxyOwner.s.sol:DeployProxyOwner \
          --rpc-url ${RPC_URL_CELO} \
          --sig "run(string)" 'celo' \
          --chain-id 42220 \
          --verifier blockscout \
          --verifier-url https://celo.blockscout.com/api/ \
          --account PK_DEPLOYER \
          --verify \
          -vv
        ignore_error: true
      - task: sync-abis
  deploy-or-upgrade-good-dollar-sybil-celo:
    desc: Deploy or upgrade good dollar sybil celo
    cmds:
      - cmd: |
          forge script script/DeployGoodDollarSybil.s.sol:DeployGoodDollarSybil \
          --rpc-url ${RPC_URL_CELO} \
          --sig "run(string)" 'celo' \
          --chain-id 42220 \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --account PK_DEPLOYER \
          --verify \
          --broadcast \
          -vv
        ignore_error: true
      - task: sync-abis
  deploy-streaming-escrow-factory-celo:
    desc: Deploy streaming escrow factory celo (set on registry factory)
    cmds:
      - cmd: |
          RESUME_FLAG=""
          if [ "${RESUME:-false}" = "true" ]; then
            RESUME_FLAG="--resume"
          fi
          forge script script/DeployOrUpgradeStreamingEscrows.s.sol:DeployOrUpgradeStreamingEscrows \
          --rpc-url ${RPC_URL_CELO} \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --sig "run(string,address)" 'celo' ${I:-0x0000000000000000000000000000000000000000} \
          --ffi \
          --broadcast \
          ${RESUME_FLAG} \
          --verify \
          --legacy \
          --chain-id 42220 \
          -vvv
  verify-streaming-escrow-factory-celo:
    desc: Verify streaming escrow factory celo
    cmds:
      - cmd: |
          bash ./scripts/verify-streaming-escrow-factory.sh \
          --network celo \
          --rpc-url ${RPC_URL_CELO} \
          --chain-id 42220 \
          --etherscan-key ${ETHERSCAN_API_KEY} \
          --verifier-url https://celo.blockscout.com/api/
  upgrade-streaming-escrows-celo:
    desc: Deploy or upgrade streaming escrows on celo (networks.json managed in script, optional I implementation)
    cmds:
      - cmd: |
          forge script script/DeployOrUpgradeStreamingEscrows.s.sol:DeployOrUpgradeStreamingEscrows \
          --rpc-url ${RPC_URL_CELO} \
          --account PK_DEPLOYER \
          --etherscan-api-key ${ETHERSCAN_API_KEY} \
          --sig "run(string,address)" 'celo' ${I:-0x0000000000000000000000000000000000000000} \
          --ffi \
          --broadcast \
          --chain-id 42220 \
          -vvv
  transfer-gas:
    desc: "Transfer native gas token from OLD_PRIVATE_KEY to PK_DEPLOYER. Usage: task transfer-gas NETWORK=<network> [AMOUNT=<native>] [PRESET_AMOUNT=<native>] [DRY_RUN=true]"
    cmds:
      - cmd: |
          network="{{.NETWORK}}"
          amount_override="{{.AMOUNT}}"
          preset_amount="{{.PRESET_AMOUNT}}"
          dry_run_flag="{{default "false" .DRY_RUN}}"

          : "${network:?missing NETWORK}"
          : "${OLD_PRIVATE_KEY:?missing OLD_PRIVATE_KEY}"
          case "${network}" in
            arbsepolia) rpc="${RPC_URL_ARB_TESTNET}"; symbol="ETH"; buffer="0.0002" ;;
            sepolia) rpc="${RPC_URL_SEP_TESTNET}"; symbol="ETH"; buffer="0.00005" ;;
            opsepolia) rpc="${RPC_URL_OP_TESTNET}"; symbol="ETH"; buffer="0.0002" ;;
            arbitrum) rpc="${RPC_URL_ARB}"; symbol="ETH"; buffer="0.0002" ;;
            optimism) rpc="${RPC_URL_OPT}"; symbol="ETH"; buffer="0.0002" ;;
            mainnet) rpc="${RPC_URL_MAINNET}"; symbol="ETH"; buffer="0.00005" ;;
            base) rpc="${RPC_URL_BASE}"; symbol="ETH"; buffer="0.0002" ;;
            polygon) rpc="${RPC_URL_POLYGON}"; symbol="POL"; buffer="0.1" ;;
            gnosis|xdai) rpc="${RPC_URL_GNOSIS}"; symbol="xDAI"; buffer="0.01" ;;
            celo) rpc="${RPC_URL_CELO}"; symbol="CELO"; buffer="0.01" ;;
            *)
              echo "Unsupported NETWORK=${network}"
              echo "Supported: arbsepolia sepolia opsepolia arbitrum optimism mainnet base polygon gnosis xdai celo"
              exit 1
              ;;
          esac

          : "${rpc:?missing RPC for ${network}}"

          from="$(cast wallet address --private-key "${OLD_PRIVATE_KEY}")"
          to="$(cast wallet address --account PK_DEPLOYER)"
          balance="$(cast balance "${from}" --rpc-url "${rpc}")"
          gas_price="$(cast gas-price --rpc-url "${rpc}")"
          gas_limit="$(cast estimate "${to}" --value 1 --from "${from}" --rpc-url "${rpc}")"
          buffer_wei="$(cast to-wei "${buffer}" ether)"

          reserve="$(python3 -c 'import sys; gas=int(sys.argv[1]); gas_price=int(sys.argv[2]); buffer=int(sys.argv[3]); print((gas * gas_price * 12 // 10) + buffer)' "${gas_limit}" "${gas_price}" "${buffer_wei}")"

          if [ -n "${amount_override}" ]; then
            amount_wei="$(cast to-wei "${amount_override}" ether)"
          elif [ -n "${preset_amount}" ]; then
            amount_wei="$(cast to-wei "${preset_amount}" ether)"
          else
            amount_wei="$(python3 -c 'import sys; balance=int(sys.argv[1]); reserve=int(sys.argv[2]); print(balance - reserve)' "${balance}" "${reserve}")"
          fi

          python3 -c 'import sys; amount=int(sys.argv[1]); reserve=int(sys.argv[2]); balance=int(sys.argv[3]); symbol=sys.argv[4]; assert amount > 0, "Computed transfer amount is <= 0"; assert amount + reserve <= balance, f"Insufficient balance for transfer plus reserve: amount={amount} reserve={reserve} balance={balance} ({symbol})"' "${amount_wei}" "${reserve}" "${balance}" "${symbol}"

          echo "Network: ${network}"
          echo "RPC: ${rpc}"
          echo "From: ${from}"
          echo "To: ${to}"
          echo "Balance (wei): ${balance}"
          echo "Amount (wei): ${amount_wei}"
          echo "Gas limit estimate: ${gas_limit}"
          echo "Gas price (wei): ${gas_price}"
          echo "Reserve (wei): ${reserve}"

          if [ "${dry_run_flag}" = "true" ]; then
            echo "Dry run only."
            exit 0
          fi

          cast send "${to}" \
            --private-key "${OLD_PRIVATE_KEY}" \
            --rpc-url "${rpc}" \
            --value "${amount_wei}"
  transfer-gas-preset:
    desc: "Transfer an inline preset amount for NETWORK from OLD_PRIVATE_KEY to PK_DEPLOYER. Usage: task transfer-gas-preset NETWORK=<network> PRESET_AMOUNT=<native> [DRY_RUN=true]"
    cmds:
      - task: transfer-gas
        vars:
          NETWORK: "{{.NETWORK}}"
          PRESET_AMOUNT: "{{.PRESET_AMOUNT}}"
          DRY_RUN: '{{default "false" .DRY_RUN}}'
  transfer-gas-all:
    desc: "Transfer gas on all supported networks. Per-network overrides are optional; omitted amounts use sweep mode. Vars: ARBSEPOLIA_AMOUNT SEPOLIA_AMOUNT OPSEPOLIA_AMOUNT ARBITRUM_AMOUNT OPTIMISM_AMOUNT MAINNET_AMOUNT BASE_AMOUNT POLYGON_AMOUNT GNOSIS_AMOUNT CELO_AMOUNT [DRY_RUN=true]"
    cmds:
      - task: transfer-gas
        vars:
          NETWORK: "arbsepolia"
          AMOUNT: '{{default "" .ARBSEPOLIA_AMOUNT}}'
          DRY_RUN: '{{default "false" .DRY_RUN}}'
      - task: transfer-gas
        vars:
          NETWORK: "sepolia"
          AMOUNT: '{{default "" .SEPOLIA_AMOUNT}}'
          DRY_RUN: '{{default "false" .DRY_RUN}}'
      - task: transfer-gas
        vars:
          NETWORK: "opsepolia"
          AMOUNT: '{{default "" .OPSEPOLIA_AMOUNT}}'
          DRY_RUN: '{{default "false" .DRY_RUN}}'
      - task: transfer-gas
        vars:
          NETWORK: "arbitrum"
          AMOUNT: '{{default "" .ARBITRUM_AMOUNT}}'
          DRY_RUN: '{{default "false" .DRY_RUN}}'
      - task: transfer-gas
        vars:
          NETWORK: "optimism"
          AMOUNT: '{{default "" .OPTIMISM_AMOUNT}}'
          DRY_RUN: '{{default "false" .DRY_RUN}}'
      - task: transfer-gas
        vars:
          NETWORK: "mainnet"
          AMOUNT: '{{default "" .MAINNET_AMOUNT}}'
          DRY_RUN: '{{default "false" .DRY_RUN}}'
      - task: transfer-gas
        vars:
          NETWORK: "base"
          AMOUNT: '{{default "" .BASE_AMOUNT}}'
          DRY_RUN: '{{default "false" .DRY_RUN}}'
      - task: transfer-gas
        vars:
          NETWORK: "polygon"
          AMOUNT: '{{default "" .POLYGON_AMOUNT}}'
          DRY_RUN: '{{default "false" .DRY_RUN}}'
      - task: transfer-gas
        vars:
          NETWORK: "gnosis"
          AMOUNT: '{{default "" .GNOSIS_AMOUNT}}'
          DRY_RUN: '{{default "false" .DRY_RUN}}'
      - task: transfer-gas
        vars:
          NETWORK: "celo"
          AMOUNT: '{{default "" .CELO_AMOUNT}}'
          DRY_RUN: '{{default "false" .DRY_RUN}}'
  transfer-gas-testnets:
    desc: "Transfer gas on testnets only. Per-network overrides are optional; omitted amounts use sweep mode. Vars: ARBSEPOLIA_AMOUNT SEPOLIA_AMOUNT OPSEPOLIA_AMOUNT [DRY_RUN=true]"
    cmds:
      - task: transfer-gas
        vars:
          NETWORK: "arbsepolia"
          AMOUNT: '{{default "" .ARBSEPOLIA_AMOUNT}}'
          DRY_RUN: '{{default "false" .DRY_RUN}}'
      - task: transfer-gas
        vars:
          NETWORK: "sepolia"
          AMOUNT: '{{default "" .SEPOLIA_AMOUNT}}'
          DRY_RUN: '{{default "false" .DRY_RUN}}'
      - task: transfer-gas
        vars:
          NETWORK: "opsepolia"
          AMOUNT: '{{default "" .OPSEPOLIA_AMOUNT}}'
          DRY_RUN: '{{default "false" .DRY_RUN}}'
  transfer-gas-mainnets:
    desc: "Transfer gas on mainnets only. Per-network overrides are optional; omitted amounts use sweep mode. Vars: ARBITRUM_AMOUNT OPTIMISM_AMOUNT MAINNET_AMOUNT BASE_AMOUNT POLYGON_AMOUNT GNOSIS_AMOUNT CELO_AMOUNT [DRY_RUN=true]"
    cmds:
      - task: transfer-gas
        vars:
          NETWORK: "arbitrum"
          AMOUNT: '{{default "" .ARBITRUM_AMOUNT}}'
          DRY_RUN: '{{default "false" .DRY_RUN}}'
      - task: transfer-gas
        vars:
          NETWORK: "optimism"
          AMOUNT: '{{default "" .OPTIMISM_AMOUNT}}'
          DRY_RUN: '{{default "false" .DRY_RUN}}'
      - task: transfer-gas
        vars:
          NETWORK: "mainnet"
          AMOUNT: '{{default "" .MAINNET_AMOUNT}}'
          DRY_RUN: '{{default "false" .DRY_RUN}}'
      - task: transfer-gas
        vars:
          NETWORK: "base"
          AMOUNT: '{{default "" .BASE_AMOUNT}}'
          DRY_RUN: '{{default "false" .DRY_RUN}}'
      - task: transfer-gas
        vars:
          NETWORK: "polygon"
          AMOUNT: '{{default "" .POLYGON_AMOUNT}}'
          DRY_RUN: '{{default "false" .DRY_RUN}}'
      - task: transfer-gas
        vars:
          NETWORK: "gnosis"
          AMOUNT: '{{default "" .GNOSIS_AMOUNT}}'
          DRY_RUN: '{{default "false" .DRY_RUN}}'
      - task: transfer-gas
        vars:
          NETWORK: "celo"
          AMOUNT: '{{default "" .CELO_AMOUNT}}'
          DRY_RUN: '{{default "false" .DRY_RUN}}'
