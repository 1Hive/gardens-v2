{
  "name": "foundry",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "make deploy",
    "fork": "make fork",
    "build": "forge build --sizes",
    "build:ir": "forge build --sizes --via-ir",
    "lint": "forge fmt",
    "test": "forge test -vvv",
    "test:force": "forge test -vvv --force",
    "format": "forge fmt"
  }
}